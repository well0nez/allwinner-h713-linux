# NFS root over the USB gigabit adapter: both drivers must be built in, not
# modules -- the network has to come up before the root filesystem, and the
# modules live on that root filesystem.
CONFIG_USB_USBNET=y
CONFIG_USB_RTL8152=y
