# TEXTS-extract.md -- the German -> English table of stage 3 (doku/121)

Package D2: `h713-mkimage`, `h713-extract` and the modules they own.  Every user-visible
string and every renamed dict key, with the file and the line it stands on after the change.
Counterpart for the installer side (package D1) is `TEXTS.md`.

Re-frozen golden values and what still carries German on purpose are at the end.

## 1. MANIFEST.json keys (h713-extract)

Structure unchanged, names translated.  `exit_code`, `device`, `device_name`, `device_status`,
`mips`, `mips.sources`, `mips.cross_check`, `mips.revision`, `sha256`, `md5` were English already.

| old key | new key | note |
|---|---|---|
| `werkzeug` | `tool` | top level |
| `zeit` | `time` | top level |
| `ergebnis` | `result` | top level |
| `device_beschreibung` | `device_description` | top level (the value is the profile's English `description` now) |
| `device_erkennung` | `device_detection` | top level |
| `device_erkennung.ueber` | `device_detection.via` |  |
| `device_erkennung.merkmale` | `device_detection.features` |  |
| `device_erkennung.hinweise` | `device_detection.notes` |  |
| `device_erkennung.stimmen` | `device_detection.votes` | only present when the board was found by content |
| `device_erkennung.stimmen.<id>.treffer` | `device_detection.votes.<id>.hits` |  |
| `device_erkennung.stimmen.<id>.widerspruch` | `device_detection.votes.<id>.against` |  |
| `referenzgeraet` | `reference_device` | top level |
| `merkmale` | `features` | top level |
| `merkmale.paket_items` | `features.package_items` |  |
| `merkmale.paket_item_sha256` | `features.package_item_sha256` |  |
| `eingang` | `input` | top level |
| `eingang.pfad` | `input.path` |  |
| `eingang.groesse` | `input.size` |  |
| `eingang.rolle` | `input.role` |  |
| `eingang.bekannt_als` | `input.known_as` |  |
| `eingang.typ` | `input.type` | values too: `eMMC-Dump`->`eMMC dump`, `Einzelteile`->`single parts`, `FAT (... bzw. ...)`->`FAT (... or ...)` |
| `eingang.teile` | `input.parts` | --part inputs |
| `eingang.teile.<k>.pfad` | `input.parts.<k>.path` |  |
| `eingang.teile.<k>.groesse` | `input.parts.<k>.size` |  |
| `eingang.imagewty.dateien` | `input.imagewty.files` |  |
| `eingang.gpt.<n>.sektoren` | `input.gpt.<n>.sectors` | raw eMMC dumps only |
| `eingang.sunxi_package.herkunft` | `input.sunxi_package.origin` |  |
| `eingang.sunxi_package.pruefsumme_ok` | `input.sunxi_package.checksum_ok` |  |
| `eingang.sunxi_package.probleme` | `input.sunxi_package.problems` |  |
| `eingang.lp.lage` | `input.lp.location` |  |
| `eingang.lp.partitionen` | `input.lp.partitions` |  |
| `eingang.lp.partitionen.<n>.gruppe` | `input.lp.partitions.<n>.group` | also the key of h713.fs.LpSuper.parts |
| `mips.quelle` | `mips.source` |  |
| `mips.dateisystem` | `mips.filesystem` |  |
| `mips.projekt_id_benutzt` | `mips.project_id_used` |  |
| `mips.projekt_id_benutzt_woher` | `mips.project_id_used_from` |  |
| `mips.projekt_id_deklariert` | `mips.project_id_declared` |  |
| `mips.projekt_id_deklariert_quelle` | `mips.project_id_declared_source` |  |
| `mips.projekt_id_deklariert_hinweis` | `mips.project_id_declared_note` |  |
| `mips.projekt_id_deklariert_benutzt` | `mips.project_id_declared_used` |  |
| `mips.projekt_id_dateien` | `mips.project_id_files` |  |
| `mips.display_bin_bekannt` | `mips.display_bin_known` |  |
| `mips.revisionstabelle` | `mips.revision_table` |  |
| `mips.nicht_extrahiert_gleiche_partition` | `mips.not_extracted_same_partition` |  |
| `artefakte` | `files` | the artefact list |
| `artefakte[].pfad` | `files[].path` |  |
| `artefakte[].groesse` | `files[].size` |  |
| `artefakte[].herkunft` | `files[].origin` |  |
| `artefakte[].pruefung` | `files[].checks` |  |
| `artefakte[].fehler` | `files[].error` |  |
| `artefakte[].referenz_stimmt` | `files[].reference_ok` |  |
| `artefakte[].referenzgeraet` | `files[].reference_device` |  |
| `nicht_extrahiert` | `not_extracted` | top level |
| `abweichungen` | `deviations` | top level |
| `warnungen` | `warnings` | top level |
| `beobachtungen` | `observations` | top level |
| `beobachtungen.tvconfig_inhalt` | `observations.tvconfig_contents` |  |
| `beobachtungen.bootloader_partition_rest` | `observations.bootloader_partition_remainder` |  |
| `beobachtungen.aic8800_dateien` | `observations.aic8800_files` |  |
| `beobachtungen.aic8800_module` | `observations.aic8800_modules` |  |
| `beobachtungen.aic8800_module[].pfad` | `observations.aic8800_modules[].path` |  |
| `beobachtungen.aic8800_module[].groesse` | `observations.aic8800_modules[].size` |  |
| `beobachtungen.hdcp_hinweise` | `observations.hdcp_notes` |  |
| `beobachtungen.hdcp_hinweise_gpt` | `observations.hdcp_notes_gpt` | raw eMMC dumps only |
| `beobachtungen.lizenzdateien` | `observations.licence_files` |  |
| `beobachtungen.notice_xml.eintraege` | `observations.notice_xml.entries` |  |
| `beobachtungen.notice_xml.aic_eintraege` | `observations.notice_xml.aic_entries` |  |
| `beobachtungen.notice_xml.fehler` | `observations.notice_xml.error` | only on a broken NOTICE.xml.gz |
| `nicht_angefasst` | `never_touched` | top level |

## 2. Dict keys inside the package (never printed, but they cross module borders)

| old key | new key | where |
|---|---|---|
| `herkunft` | `origin` | h713.extract read set (_read_mips/_read_mips_vendor) |
| `typ` | `type` | h713.extract read set |
| `art` | `kind` | h713.extract read set |
| `dateien` | `files` | h713.extract read set |
| `kurznamen` | `short_names` | h713.extract read set |
| `probleme` | `problems` | h713.extract read set |
| `uebrig` | `leftover` | h713.extract read set |
| `sonst` | `others` | h713.extract read set |
| `quelle` | `source` | h713.extract.Run.declared_project_id() |
| `hinweis` | `note` | h713.extract.Run.declared_project_id() |
| `_gesehen` | `_seen` | h713.extract.Run.detection, never written to the manifest |
| `ziel` | `target` | h713.vendorfiles.parse_mspm() block |
| `laenge` | `length` | h713.vendorfiles.parse_mspm() block |
| `paare` | `pairs` | h713.vendorfiles.parse_mspm() block |
| `kopf` | `header` | h713.vendorfiles.tse_header() |
| `gruppe` | `group` | h713.fs.LpSuper.parts |

## 3. Files the tools write

| old name | new name | note |
|---|---|---|
| `BERICHT.txt` | `REPORT.txt` | `BERICHT.txt` is still written next to it, byte-identical, for one release |
| `<stem>-LIESMICH.txt` | `<stem>-README.txt` | `h713-mkimage build`; the German file is not written any more |

## 4. Scripts

| old | new | note |
|---|---|---|
| `hy310-mkimage.py` | `h713-mkimage` | subcommands `build`/`check`/`tree-boot`/`tree-rootfs`; the old file forwards |
| `--out FILE` | `build -o FILE` | old switch still accepted, prints one line |
| `--pruefen TABLE` | `check TABLE` | as above |
| `--baum-boot DIR` | `tree-boot DIR` | as above |
| `--baum-rootfs DIR` | `tree-rootfs DIR` | as above |
| `mkimage-selbsttest.py` | `mkimage-selftest.py` | old file forwards (release/build-all.sh calls it) |
| `--behalten` | `--keep` | `--behalten` still accepted |
| `mkimage-eingaben.sh` | `mkimage-inputs.sh` | old file forwards (release/build-all.sh calls it) |
| `WURZEL=` | `ROOT=` | `WURZEL=` still accepted |

## 5. Strings, file by file (345 of them)

| file:line | old | new |
|---|---|---|
| `h713/util.py:48` | f"sha256 über {n} B in {time.time() - t0:.1f} s" | f"sha256 over {n} B in {time.time() - t0:.1f} s" |
| `h713/source.py:52` | f"{name}: Bereich {off:#x}+{size:#x} liegt außerhalb von {parent.name} ({parent.size:#x} B)" | f"{name}: range {off:#x}+{size:#x} lies outside {parent.name} ({parent.size:#x} B)" |
| `h713/source.py:83` | "kein Sparse-Abbild" | "not a sparse image" |
| `h713/source.py:85` | f"Sparse-Kopf ungewöhnlich: v{maj}.{mi}, Kopf {fhs}, Chunk-Kopf {chs}" | f"unusual sparse header: v{maj}.{mi}, header {fhs}, chunk header {chs}" |
| `h713/source.py:99` | f"Sparse-Chunk {i}: RAW-Länge passt nicht ({tsz} vs {12 + ln})" | f"sparse chunk {i}: RAW length does not fit ({tsz} vs {12 + ln})" |
| `h713/source.py:111` | f"Sparse: Chunks decken {lblk} Blöcke, Kopf sagt {self.total_blks}" | f"sparse: the chunks cover {lblk} blocks, the header says {self.total_blks}" |
| `h713/source.py:112` | f"Sparse v{maj}.{mi}, Block {self.blk}, {self.total_blks} Blöcke = {self.size} B logisch, " | f"sparse v{maj}.{mi}, block {self.blk}, {self.total_blks} blocks = {self.size} B logical, " |
| `h713/source.py:227` | f"{q.name} -> {target.name}: {q.size} B in {time.time() - t0:.1f} s (Zwischenstand, wird gelöscht)" | f"{q.name} -> {target.name}: {q.size} B in {time.time() - t0:.1f} s (scratch copy, deleted at the end)" |
| `h713/fs/sparse.py:46` | "Sparse-Version %d wird nicht unterstuetzt" % maj | "sparse version %d is not supported" % maj |
| `h713/fs/sparse.py:48` | "Sparse-Blockgroesse %d ist kein Vielfaches von %d" % (blk, SECTOR) | "sparse block size %d is not a multiple of %d" % (blk, SECTOR) |
| `h713/fs/sparse.py:90` | "unbekannter Sparse-Stuecktyp 0x%04x" % ctype | "unknown sparse chunk type 0x%04x" % ctype |
| `h713/fs/sparse.py:94` | "Sparse-Abbild unvollstaendig: %d von %d Bloecken" % (block, nblk) | "sparse image incomplete: %d of %d blocks" % (block, nblk) |
| `h713/gpt.py:83` | f"GPT: Kopf-CRC {'ok' if self.header_ok else 'FALSCH'}, Tabellen-CRC {'ok' if self.table_ok else 'FALSCH'}, " f"{pe_n} Einträge à {pe_sz} B ab LBA {pe_lba}, nutzbar {first}..{last}, Backup-Kopf LBA {bak}" | f"GPT: header CRC {'ok' if self.header_ok else 'WRONG'}, table CRC {'ok' if self.table_ok else 'WRONG'}, " f"{pe_n} entries of {pe_sz} B from LBA {pe_lba}, usable {first}..{last}, backup header LBA {bak}" |
| `h713/gpt.py:85` | log.info("Partitionen: " + ", ".join( | log.info("partitions: " + ", ".join( |
| `h713/gpt.py:94` | f"Partition {name} (LBA {s}, {c} Sektoren)" | f"partition {name} (LBA {s}, {c} sectors)" |
| `h713/gpt.py:255` | "Schutz-MBR: erster Eintrag ist nicht Typ 0xEE" | "protective MBR: the first entry is not type 0xEE" |
| `h713/gpt.py:258` | "primaerer GPT-Kopf ohne Signatur" | "primary GPT header without a signature" |
| `h713/gpt.py:265` | "CRC des primaeren GPT-Kopfs stimmt nicht" | "CRC of the primary GPT header does not match" |
| `h713/gpt.py:269` | "MyLBA/AlternateLBA falsch (%d/%d)" | "MyLBA/AlternateLBA wrong (%d/%d)" |
| `h713/gpt.py:271` | "FirstUsableLBA ist %d, erwartet %d" | "FirstUsableLBA is %d, expected %d" |
| `h713/gpt.py:273` | "LastUsableLBA ist %d, erwartet %d" | "LastUsableLBA is %d, expected %d" |
| `h713/gpt.py:275` | "%d Eintraege a %d Byte, erwartet %d a %d" | "%d entries of %d bytes, expected %d of %d" |
| `h713/gpt.py:278` | "CRC der Partitionstabelle stimmt nicht" | "CRC of the partition table does not match" |
| `h713/gpt.py:293` | "Sicherungskopie des GPT-Kopfs fehlt am Plattenende (Befund S46 B7)" | "the backup copy of the GPT header is missing at the end of the disk (finding S46 B7)" |
| `h713/gpt.py:298` | "Sicherungskopf: MyLBA/AlternateLBA falsch" | "backup header: MyLBA/AlternateLBA wrong" |
| `h713/gpt.py:301` | "Sicherungstabelle weicht von der primaeren ab" | "the backup table differs from the primary one" |
| `h713/gpt.py:307` | "CRC des Sicherungskopfs stimmt nicht" | "CRC of the backup header does not match" |
| `h713/fex.py:49` | "sys_partition.fex fehlt im Image -- kein Allwinner-Vollimage?" | "sys_partition.fex is missing from the image -- not a full Allwinner image?" |
| `h713/fs/lpsuper.py:37` | f"LP-Geometrie @{goff} gültig (sha256 ok)" | f"LP geometry @{goff} valid (sha256 ok)" |
| `h713/fs/lpsuper.py:39` | f"LP-Geometrie @{goff}: Prüfsumme falsch" | f"LP geometry @{goff}: wrong checksum" |
| `h713/fs/lpsuper.py:41` | "keine gültige LP-Geometrie bei 4096/8192 — ist das eine 'super'-Partition?" | "no valid LP geometry at 4096/8192 -- is this a 'super' partition?" |
| `h713/fs/lpsuper.py:45` | ("primär", base) | ("primary", base) |
| `h713/fs/lpsuper.py:56` | "keine gültigen LP-Metadaten" | "no valid LP metadata" |
| `h713/fs/lpsuper.py:69` | "Kopf-Prüfsumme falsch" | "wrong header checksum" |
| `h713/fs/lpsuper.py:78` | "Tabellen-Prüfsumme falsch" | "wrong table checksum" |
| `h713/fs/ext4.py:112` | f"{label}: unsinnige Blockgröße im Superblock (s_log_block_size {log_bs})" | f"{label}: nonsensical block size in the superblock (s_log_block_size {log_bs})" |
| `h713/fs/ext4.py:129` | f"ext4, Block {self.block_size}, {s_blocks_lo} Blöcke = {s_blocks_lo * self.block_size} B, " f"Label '{vol}', feature_incompat {self.feat_incompat:#x}" | f"ext4, block {self.block_size}, {s_blocks_lo} blocks = {s_blocks_lo * self.block_size} B, " f"label '{vol}', feature_incompat {self.feat_incompat:#x}" |
| `h713/fs/ext4.py:134` | f"{label}: unbrauchbarer Superblock (inodes/Gruppe {self.inodes_per_group}, " f"Blöcke/Gruppe {self.blocks_per_group}, Inode-Größe {self.inode_size})" | f"{label}: unusable superblock (inodes/group {self.inodes_per_group}, " f"blocks/group {self.blocks_per_group}, inode size {self.inode_size})" |
| `h713/fs/ext4.py:137` | f"{label}: unsinnige Inode-Größe {self.inode_size}" | f"{label}: nonsensical inode size {self.inode_size}" |
| `h713/fs/ext4.py:141` | f"{label}: ext4-Merkmal '{what}' wird von diesem Leser nicht unterstützt " f"(feature_incompat {self.feat_incompat:#x}) — mit --use-debugfs versuchen" | f"{label}: ext4 feature '{what}' is not supported by this reader " f"(feature_incompat {self.feat_incompat:#x}) -- try --use-debugfs" |
| `h713/fs/ext4.py:144` | f"{label}: ext4-Merkmal 'bigalloc' wird von diesem Leser nicht unterstützt " f"— mit --use-debugfs versuchen" | f"{label}: ext4 feature 'bigalloc' is not supported by this reader " f"-- try --use-debugfs" |
| `h713/fs/ext4.py:150` | f"{label}: unsinnige Gruppendeskriptorgröße {self.desc_size}" | f"{label}: nonsensical group descriptor size {self.desc_size}" |
| `h713/fs/ext4.py:158` | f"{label}: Gruppendeskriptoren unvollständig ({len(gd)} von " f"{self.groups * self.desc_size} B) — Bereich zu klein?" | f"{label}: group descriptors incomplete ({len(gd)} of " f"{self.groups * self.desc_size} B) -- range too small?" |
| `h713/fs/ext4.py:166` | f"{self.groups} Gruppe(n) à {self.blocks_per_group} Blöcke, {self.inodes_per_group} Inodes; " f"Inode-Größe {self.inode_size}, Deskriptor {self.desc_size} B" | f"{self.groups} group(s) of {self.blocks_per_group} blocks, {self.inodes_per_group} inodes; " f"inode size {self.inode_size}, descriptor {self.desc_size} B" |
| `h713/fs/ext4.py:166` | f"{self.groups} group(s) of {self.blocks_per_group} blocks, {self.inodes_per_group} inodes; " f"inode size {self.inode_size}, descriptor {self.desc_size} B" f"{' (64bit)' if self.has_64bit else ''}, {self.inodes_count} Inodes, UUID {self.uuid}" | f"{self.groups} group(s) of {self.blocks_per_group} blocks, {self.inodes_per_group} inodes; " f"inode size {self.inode_size}, descriptor {self.desc_size} B" f"{' (64bit)' if self.has_64bit else ''}, {self.inodes_count} inodes, UUID {self.uuid}" |
| `h713/fs/ext4.py:187` | f"{self.label}: Block {nr} liegt außerhalb des Dateisystems ({self.blocks_count} Blöcke)" | f"{self.label}: block {nr} lies outside the file system ({self.blocks_count} blocks)" |
| `h713/fs/ext4.py:194` | f"{self.label}: Quelle endet bei {p + len(b)} B, Block {nr}+{count} reicht bis " f"{(nr + count) * self.block_size} B — fehlender Rest wird als Nullen gelesen" | f"{self.label}: the source ends at {p + len(b)} B, block {nr}+{count} reaches to " f"{(nr + count) * self.block_size} B -- the missing rest is read as zeros" |
| `h713/fs/ext4.py:218` | f"{self.label}: Inode {ino} liegt außerhalb des Dateisystems (1..{self.inodes_count})" | f"{self.label}: inode {ino} lies outside the file system (1..{self.inodes_count})" |
| `h713/fs/ext4.py:223` | f"{self.label}: Inode {ino} nicht lesbar (Tabelle außerhalb der Quelle?)" | f"{self.label}: inode {ino} not readable (table outside the source?)" |
| `h713/fs/ext4.py:252` | f"{self.label}: Inode {ino}: Extent-Knoten zu kurz" | f"{self.label}: inode {ino}: extent node too short" |
| `h713/fs/ext4.py:255` | f"{self.label}: Inode {ino}: Extent-Kopf ohne Magic 0xF30A (ist {magic:#06x})" | f"{self.label}: inode {ino}: extent header without magic 0xF30A (it is {magic:#06x})" |
| `h713/fs/ext4.py:259` | f"{self.label}: Inode {ino}: {entries} Extent-Einträge passen nicht in den Knoten" | f"{self.label}: inode {ino}: {entries} extent entries do not fit into the node" |
| `h713/fs/ext4.py:300` | f"{self.label}: Inode {ino}: inline_data wird von diesem Leser nicht unterstützt" | f"{self.label}: inode {ino}: inline_data is not supported by this reader" |
| `h713/fs/ext4.py:302` | f"{self.label}: Inode {ino}: verschlüsselt — wird nicht gelesen" | f"{self.label}: inode {ino}: encrypted -- not read" |
| `h713/fs/ext4.py:445` | f"{self.label}: {path} nicht gefunden" | f"{self.label}: {path} not found" |
| `h713/fs/ext4.py:448` | f"{self.label}: {path} ist ein Verzeichnis" | f"{self.label}: {path} is a directory" |
| `h713/fs/ext4.py:453` | f"{self.structure}; gelesen: {s['inodes']} Inodes ({s['extent_inodes']} mit Extents, " f"{s['indirekt_inodes']} indirekt), {s['extents']} Extents in {s['extent_knoten']} Knoten, " f"Baumtiefe max {s['extent_max_tiefe']}, {s['unbelegte_extents']} unbelegt, " f"{s['verzeichnisse']} Verzeichnisse ({s['htree_verzeichnisse']} mit dir_index-Flag), " f"{s['symlink_schnell']}+{s['symlink_block']} Symlinks (schnell+in Blöcken), " f"{s['loecher']} Dateien mit Löchern, {s['gelesene_bytes']} B aus der Quelle geholt" | f"{self.structure}; read: {s['inodes']} inodes ({s['extent_inodes']} with extents, " f"{s['indirekt_inodes']} indirect), {s['extents']} extents in {s['extent_knoten']} nodes, " f"tree depth max {s['extent_max_tiefe']}, {s['unbelegte_extents']} unallocated, " f"{s['verzeichnisse']} directories ({s['htree_verzeichnisse']} with the dir_index flag), " f"{s['symlink_schnell']}+{s['symlink_block']} symlinks (fast+in blocks), " f"{s['loecher']} files with holes, {s['gelesene_bytes']} B fetched from the source" |
| `h713/fs/ext4.py:129` | f"ext4, Block {self.block_size}, {s_blocks_lo} Blöcke = {s_blocks_lo * self.block_size} B, " f"Label '{vol}', feature_incompat {feat_incompat:#x}" | f"ext4, block {self.block_size}, {s_blocks_lo} blocks = {s_blocks_lo * self.block_size} B, " f"label '{vol}', feature_incompat {feat_incompat:#x}" |
| `h713/fs/ext4.py:482` | "debugfs (e2fsprogs) nicht gefunden — nötig für --use-debugfs (ohne die Option " "liest h713-extract ext4 selbst)" | "debugfs (e2fsprogs) not found -- needed for --use-debugfs (without that option " "h713-extract reads ext4 itself)" |
| `h713/fs/ext4.py:490` | "debugfs kann '?offset=' hier nicht — Partition wird zwischengespeichert" | "debugfs cannot do '?offset=' here -- the partition is cached as a file" |
| `h713/fs/ext4.py:543` | f"debugfs konnte {path} nicht lesen" | f"debugfs could not read {path}" |
| `h713/fs/fat16.py:43` | f"{origin}: zu klein für einen FAT-Bootsektor" | f"{origin}: too small for a FAT boot sector" |
| `h713/fs/fat16.py:53` | f"{origin}: kein brauchbarer FAT-BPB (bps {self.bps}, spc {self.spc}, fats {self.nfats})" | f"{origin}: no usable FAT BPB (bps {self.bps}, spc {self.spc}, fats {self.nfats})" |
| `h713/fs/fat16.py:70` | f"{self.type}, {self.bps} B/Sektor, {self.spc} Sektoren/Cluster ({self.cluster_bytes} B), " f"{self.nfats} FATs à {self.fatsz} Sektoren, Wurzel {self.root_entries} Einträge, " f"{self.total} Sektoren = {self.total * self.bps} B, {self.cluster_count} Cluster, " f"Label '{self.label}'" | f"{self.type}, {self.bps} B/sector, {self.spc} sectors/cluster ({self.cluster_bytes} B), " f"{self.nfats} FATs of {self.fatsz} sectors, root {self.root_entries} entries, " f"{self.total} sectors = {self.total * self.bps} B, {self.cluster_count} clusters, " f"label '{self.label}'" |
| `h713/fs/fat16.py:79` | f"FAT-Volumen behauptet {self.total * self.bps} B, die Quelle hat {q.size} B — " f"es wird nur gelesen, was vorhanden ist" | f"the FAT volume claims {self.total * self.bps} B, the source has {q.size} B -- " f"only what is there is read" |
| `h713/fs/fat16.py:85` | f"{origin}: FAT-Tabelle nicht lesbar" | f"{origin}: FAT table not readable" |
| `h713/fs/fat16.py:109` | f"{self.origin}: Clusterkette läuft im Kreis bei {c}" | f"{self.origin}: the cluster chain loops at {c}" |
| `h713/fs/fat16.py:196` | f"{self.origin}: {e['name']}: Cluster {c} liegt außerhalb des Volumens" | f"{self.origin}: {e['name']}: cluster {c} lies outside the volume" |
| `h713/fs/fat16.py:205` | f"{self.origin}: {e['name']}: Clusterkette endet nach {len(d)} von {e['groesse']} B" | f"{self.origin}: {e['name']}: the cluster chain ends after {len(d)} of {e['groesse']} B" |
| `h713/fs/ext4.py:104` | raise Abort(f"{label}: kein ext4-Superblock (Magic bei 0x438 fehlt) — erofs/f2fs? Nicht unterstützt.") | raise Abort(f"{label}: no ext4 superblock (no magic at 0x438) -- erofs/f2fs? Not supported.") |
| `h713/fs/fat16.py:199` | f"{self.origin}: {e['name']}: Quelle endet mitten in Cluster {c} " f"({got + len(b)} von {e['groesse']} B) — Abbild abgeschnitten?" | f"{self.origin}: {e['name']}: the source ends in the middle of cluster {c} " f"({got + len(b)} of {e['groesse']} B) -- image truncated?" |
| `h713/imagewty.py:36` | "IMAGEWTY v1-Kopf (0x0100) — Layout aus awimage übernommen, hier ungetestet" | "IMAGEWTY v1 header (0x0100) -- layout taken from awimage, untested here" |
| `h713/imagewty.py:38` | f"image_size {self.image_size} B (Datei {q.size} B), image_header_size {self.image_header_size:#x}, " f"pid {self.pid:#x} vid {self.vid:#x} hw {self.hardware_id:#x} fw {self.firmware_id:#x}, {self.num_files} Dateien" | f"image_size {self.image_size} B (file {q.size} B), image_header_size {self.image_header_size:#x}, " f"pid {self.pid:#x} vid {self.vid:#x} hw {self.hardware_id:#x} fw {self.firmware_id:#x}, {self.num_files} files" |
| `h713/imagewty.py:41` | f"image_header_size {self.image_header_size:#x} statt 0x400 — Tabellenlayout unsicher" | f"image_header_size {self.image_header_size:#x} instead of 0x400 -- table layout uncertain" |
| `h713/imagewty.py:43` | f"IMAGEWTY: unplausible Dateizahl {self.num_files}" | f"IMAGEWTY: implausible file count {self.num_files}" |
| `h713/imagewty.py:49` | "Bereich 0x60..0x3ff: Füllmuster n² mod 256 (kein Chiffrat, keine Nutzdaten)" | "range 0x60..0x3ff: fill pattern n^2 mod 256 (no cipher text, no payload)" |
| `h713/imagewty.py:59` | "IMAGEWTY: Dateitabelle abgeschnitten" | "IMAGEWTY: file table truncated" |
| `h713/imagewty.py:71` | f"IMAGEWTY: Dateitabelle bei {tab + i * 0x400:#x} unlesbar (total_header_size {total_header_size:#x}, " f"filename_len {filename_len:#x}) — verschlüsselter Container (RC6)? Dann extern entpacken und --fex-dir nutzen." | f"IMAGEWTY: file table at {tab + i * 0x400:#x} unreadable (total_header_size {total_header_size:#x}, " f"filename_len {filename_len:#x}) -- encrypted container (RC6)? Then unpack it externally and use --fex-dir." |
| `h713/imagewty.py:74` | f"IMAGEWTY: {fn}: Offset {off:#x} + {stored} überschreitet das Image oder original>stored" | f"IMAGEWTY: {fn}: offset {off:#x} + {stored} runs past the image, or original>stored" |
| `h713/imagewty.py:79` | log.info("Dateien: " + ", ".join(self.entries)) | log.info("files: " + ", ".join(self.entries)) |
| `h713/imagewty.py:81` | f"letzte Nutzdaten enden bei {end:#x} ({end} B); Kopf image_size {self.image_size}, Datei {q.size}" | f"the last payload ends at {end:#x} ({end} B); header image_size {self.image_size}, file {q.size}" |
| `h713/imagewty.py:83` | f"Image abgeschnitten: Nutzdaten reichen bis {end}, Datei hat nur {q.size} B" | f"image truncated: the payload reaches to {end}, the file has only {q.size} B" |
| `h713/imagewty.py:85` | f"Kopf image_size weicht um {self.image_size - q.size:+d} B von der Dateigröße ab — Nutzdaten sind vollständig, " f"nur Beobachtung" | f"the header image_size differs from the file size by {self.image_size - q.size:+d} B -- the payload is complete, " f"observation only" |
| `h713/imagewty.py:114` | f"{origin}: kein sunxi-package bei {off:#x}" | f"{origin}: no sunxi-package at {off:#x}" |
| `h713/imagewty.py:118` | f"Magic {magic:#x} statt 0x89119800" | f"magic {magic:#x} instead of 0x89119800" |
| `h713/imagewty.py:120` | f"Kopf-Ende {h[0x3C:0x40]!r} statt 'MIE;'" | f"header end {h[0x3C:0x40]!r} instead of 'MIE;'" |
| `h713/imagewty.py:122` | f"{origin}: items_nr {items_nr} unplausibel" | f"{origin}: items_nr {items_nr} implausible" |
| `h713/imagewty.py:124` | f"{origin}: valid_len {valid_len:#x} unplausibel" | f"{origin}: valid_len {valid_len:#x} implausible" |
| `h713/imagewty.py:131` | f"add_sum {add_sum:#x}, gerechnet {total:#x}" | f"add_sum {add_sum:#x}, computed {total:#x}" |
| `h713/imagewty.py:139` | f"Item {i} ({name}): Ende {bytes(e[0x16C:0x170])!r} statt 'IIE;'" | f"item {i} ({name}): end {bytes(e[0x16C:0x170])!r} instead of 'IIE;'" |
| `h713/imagewty.py:141` | f"Item {name}: {doff:#x}+{dlen} überschreitet valid_len" | f"item {name}: {doff:#x}+{dlen} runs past valid_len" |
| `h713/imagewty.py:143` | f"Item {name}: encrypt={enc}" | f"item {name}: encrypt={enc}" |
| `h713/imagewty.py:147` | f"{origin}: sunxi-package @{off:#x}, {items_nr} Items, valid_len {valid_len:#x}, " f"Prüfsumme {'ok' if self.checksum_ok else 'FALSCH'}: " | f"{origin}: sunxi-package @{off:#x}, {items_nr} items, valid_len {valid_len:#x}, " f"checksum {'ok' if self.checksum_ok else 'WRONG'}: " |
| `h713/imagewty.py:223` | f"Länge {len(scp)} nicht durch 4 teilbar" | f"length {len(scp)} is not divisible by 4" |
| `h713/imagewty.py:225` | "Blob ist leer (nur Nullen)" | "the blob is empty (zeros only)" |
| `h713/imagewty.py:232` | f"Reset-Vektor @0x100 = l.j {target:#x} (OR1K, plausibel)" | f"reset vector @0x100 = l.j {target:#x} (OR1K, plausible)" |
| `h713/imagewty.py:234` | f"Reset-Vektor @0x100 ist kein l.j (Opcode {op:#x}) — Byte-Ordnung oder Blob fraglich" | f"reset vector @0x100 is not an l.j (opcode {op:#x}) -- byte order or blob doubtful" |
| `h713/imagewty.py:238` | "Parameterkopf @0x4000: 'CPUs' vorhanden" | "parameter header @0x4000: 'CPUs' present" |
| `h713/imagewty.py:240` | f"Parameterkopf @0x4004 ist {head[4:8]!r}, nicht 'CPUs'" | f"parameter header @0x4004 is {head[4:8]!r}, not 'CPUs'" |
| `h713/imagewty.py:245` | "Versionsstring: '" + m.group(0).decode("latin1").strip() + "'" | "version string: '" + m.group(0).decode("latin1").strip() + "'" |
| `h713/imagewty.py:247` | "kein 'ARISC'-Versionsstring gefunden" | "no 'ARISC' version string found" |
| `h713/imagewty.py:253` | f"{nops} wortausgerichtete l.nop in den ersten 128 KiB" | f"{nops} word-aligned l.nop in the first 128 KiB" |
| `h713/vendorfiles.py:92` | "keine ELF-Datei" | "not an ELF file" |
| `h713/vendorfiles.py:153` | f"bei +{o:#x}: kein MSPM-Kopf ({h[:4]!r})" | f"at +{o:#x}: no MSPM header ({h[:4]!r})" |
| `h713/vendorfiles.py:158` | f"Block {len(blocks)} bei +{o:#x}: Kopffelder {zero:#x} {target:#x} {raw:#x} unerwartet" | f"block {len(blocks)} at +{o:#x}: header fields {zero:#x} {target:#x} {raw:#x} unexpected" |
| `h713/vendorfiles.py:160` | f"Block {len(blocks)} bei +{o:#x}: Länge {length} passt nicht" | f"block {len(blocks)} at +{o:#x}: length {length} does not fit" |
| `h713/vendorfiles.py:166` | f"Kette endet bei +{o:#x}, Blob hat {len(blob)} B" | f"the chain ends at +{o:#x}, the blob has {len(blob)} B" |
| `h713/vendorfiles.py:168` | "kein einziger MSPM-Block" | "not a single MSPM block" |
| `h713/vendorfiles.py:201` | f"{who}: EDID-Kopf fehlt ({hexdump_short(b, 8)})" | f"{who}: EDID header missing ({hexdump_short(b, 8)})" |
| `h713/vendorfiles.py:205` | f"{who}: Block {i // 128} Prüfsumme {s:#04x} statt 0" | f"{who}: block {i // 128} checksum {s:#04x} instead of 0" |
| `h713/vendorfiles.py:207` | f"{who}: Erweiterungszähler {b[126]} statt 1" | f"{who}: extension counter {b[126]} instead of 1" |
| `h713/vendorfiles.py:209` | f"{who}: Block 1 ist kein CEA-861 (Tag {b[128]:#04x})" | f"{who}: block 1 is not CEA-861 (tag {b[128]:#04x})" |
| `h713/vendorfiles.py:240` | "[CONFIG] picture_mode fehlt" | "[CONFIG] picture_mode missing" |
| `h713/vendorfiles.py:244` | f"Sektion [{input_name}] fehlt" | f"section [{input_name}] missing" |
| `h713/vendorfiles.py:249` | f"[{input_name}] Modus {m} fehlt" | f"[{input_name}] mode {m} missing" |
| `h713/vendorfiles.py:255` | "[PQ_ENABLE] fehlt" | "[PQ_ENABLE] missing" |
| `h713/vendorfiles.py:257` | "[PICTURE_CURVE_HDMI] fehlt" | "[PICTURE_CURVE_HDMI] missing" |
| `h713/vendorfiles.py:263` | f"[PICTURE_CURVE_HDMI] {k} fehlt" | f"[PICTURE_CURVE_HDMI] {k} missing" |
| `h713/vendorfiles.py:265` | f"[PICTURE_CURVE_HDMI] {k}: {len(ini_numbers(d[k]))} statt 5 Stützstellen" | f"[PICTURE_CURVE_HDMI] {k}: {len(ini_numbers(d[k]))} instead of 5 support points" |
| `h713/vendorfiles.py:269` | "[COLOR_TEMP_HDMI] fehlt" | "[COLOR_TEMP_HDMI] missing" |
| `h713/vendorfiles.py:274` | f"[COLOR_TEMP_HDMI] {k} fehlt" | f"[COLOR_TEMP_HDMI] {k} missing" |
| `h713/vendorfiles.py:280` | "[HDMIOverscanSetting] fehlt" | "[HDMIOverscanSetting] missing" |
| `h713/vendorfiles.py:286` | "<transform><item name=\"gamma\"> fehlt" | "<transform><item name=\"gamma\"> missing" |
| `h713/vendorfiles.py:288` | "gamma level0..level4 unvollständig" | "gamma level0..level4 incomplete" |
| `h713/vendorfiles.py:291` | "kein SQLite-Kopf" | "no SQLite header" |
| `h713/vendorfiles.py:300` | f"Tabelle {t} fehlt" | f"table {t} missing" |
| `h713/vendorfiles.py:304` | f"Tabelle {t} leer" | f"table {t} empty" |
| `h713/vendorfiles.py:311` | "keine dreispaltigen Port-Zeilen" | "no three-column port rows" |
| `h713/vendorfiles.py:313` | "kein HDMI-Port" | "no HDMI port" |
| `h713/vendorfiles.py:315` | f"nicht lesbar: {e}" | f"not readable: {e}" |
| `h713/vendorfiles.py:337` | f"{name}: TSE-Magic fehlt (Kopf {k['kopf']})" | f"{name}: TSE magic missing (header {k['header']})" |
| `h713/vendorfiles.py:343` | f"{name}: Kopf sagt ID {k['id']:#06x}, der Dateiname sagt {from_name:#06x} — passt nicht zusammen" | f"{name}: the header says ID {k['id']:#06x}, the file name says {from_name:#06x} -- they do not match" |
| `h713/vendorfiles.py:345` | f"{name}: TSE-Kopf {k['kopf']}, ID {k['id']:#06x} = Dateiname" | f"{name}: TSE header {k['header']}, ID {k['id']:#06x} = file name" |
| `h713/vendorfiles.py:347` | f"{name}: TSE-Kopf {k['kopf']}, ID-Feld {k['id']:#06x}" | f"{name}: TSE header {k['header']}, ID field {k['id']:#06x}" |
| `h713/vendorfiles.py:388` | "display_cfg.xml: endet auf Null-Byte(s) hinter </root> (Stock-Eigenart, unverändert kopiert)" | "display_cfg.xml: ends on null byte(s) behind </root> (a stock quirk, copied unchanged)" |
| `h713/vendorfiles.py:393` | f"display_cfg.xml: nicht parsebar ({e})" | f"display_cfg.xml: not parsable ({e})" |
| `h713/vendorfiles.py:395` | f"display_cfg.xml: wohlgeformt, Wurzel <{root.tag}>, {len(children)} Kinder" | f"display_cfg.xml: well formed, root <{root.tag}>, {len(children)} children" |
| `h713/extract.py:4` | `Run` is the old `Lauf` of h713-extract (X:1999-3103); `main()` stays in the CLI script. Stage 1 of doku/121: identifiers, comments and docstrings are English, every user-visible string (log lines, MANIFEST.json keys, BERICHT.txt, exit codes) is byte-identical. """ | `Run` is the old `Lauf` of h713-extract (X:1999-3103); `main()` stays in the CLI script. Stage 3 of doku/121: every user-visible string is English now -- log lines, warnings, the MANIFEST.json keys and the report. The report is written as REPORT.txt, and for one release BERICHT.txt is written next to it as a byte-identical copy (api-stufe3.md). The old -> new text and key tables are in installer/tests/TEXTS-extract.md. """ |
| `h713/extract.py:44` | VERSION = "0.4 (S45: eigener ext4-Leser, kein debugfs mehr; ext4 über e2fsprogs nur noch mit --use-debugfs)" | VERSION = "0.4 (S45: own ext4 reader, no debugfs any more; ext4 through e2fsprogs only with --use-debugfs)" |
| `h713/extract.py:84` | self.detection: Dict[str, object] = {"ueber": None, "merkmale": [], "hinweise": []} | self.detection: Dict[str, object] = {"via": None, "features": [], "notes": []} |
| `h713/extract.py:90` | self.deviations: List[str] = [] # differences from the stock of the reference device (unknown: „hier drohen Probleme") | self.deviations: List[str] = [] # differences from the stock of the reference device |
| `h713/extract.py:105` | raise Abort(f"Eingang nicht gefunden: {path}") | raise Abort(f"input not found: {path}") |
| `h713/extract.py:111` | self.log.heading(f"Fingerabdruck {role}: {q.path} ({q.size} B)") | self.log.heading(f"fingerprint {role}: {q.path} ({q.size} B)") |
| `h713/extract.py:113` | self.log.info("sha256 übersprungen (--no-hash) — Gerät wird nur inhaltlich erkannt") | self.log.info("sha256 skipped (--no-hash) -- the device is recognised by content alone") |
| `h713/extract.py:118` | self.input_facts.update({"pfad": str(q.path), "groesse": q.size, "sha256": h, "rolle": role}) | self.input_facts.update({"path": str(q.path), "size": q.size, "sha256": h, "role": role}) |
| `h713/extract.py:121` | self.input_facts["bekannt_als"] = known[1] self.log.info(f"BEKANNT: {known[1]}") self.set_device(known[0], "sha256", fix=True) else: self.input_facts["bekannt_als"] = None self.log.info("Image-Fingerabdruck unbekannt — Gerät wird inhaltlich bestimmt (Paket-Hashes, U-Boot/ARISC-Kennung, " "Vendor-Fingerprint); passt kein Profil, bleibt das Image unbekannt (Best-Effort, Exit 1)") | self.input_facts["known_as"] = known[1] self.log.info(f"KNOWN: {known[1]}") self.set_device(known[0], "sha256", fix=True) else: self.input_facts["known_as"] = None self.log.info("image fingerprint unknown -- the device is determined from the content (package hashes, " "U-Boot/ARISC identifier, vendor fingerprint); if no profile fits, the image stays " "unknown (best effort, exit 1)") |
| `h713/extract.py:131` | self.input_facts["typ"] = "IMAGEWTY" self.log.heading("IMAGEWTY-Container") img = Imagewty(q, self.log) self.input_facts["imagewty"] = {"header_version": img.header_version, "num_files": img.num_files, "dateien": {n: {"maintype": e["maintype"], "subtype": e["subtype"], "offset": e["offset"], "original": e["original"], "stored": e["stored"]} for n, e in img.entries.items()}} bp = img.file("boot_package.fex") if bp is None: self.log.warn("boot_package.fex fehlt im Image — kein scp.bin aus diesem Eingang") else: self.package_from_source(bp, "boot_package.fex im Image") | self.input_facts["type"] = "IMAGEWTY" self.log.heading("IMAGEWTY container") img = Imagewty(q, self.log) self.input_facts["imagewty"] = {"header_version": img.header_version, "num_files": img.num_files, "files": {n: {"maintype": e["maintype"], "subtype": e["subtype"], "offset": e["offset"], "original": e["original"], "stored": e["stored"]} for n, e in img.entries.items()}} bp = img.file("boot_package.fex") if bp is None: self.log.warn("boot_package.fex is missing from the image -- no scp.bin out of this input") else: self.package_from_source(bp, "boot_package.fex in the image") |
| `h713/extract.py:146` | self.log.info(f"toc1.fex: {t1.size} B ({'Platzhalter' if t1.size < 1024 else 'echtes TOC1?'})") | self.log.info(f"toc1.fex: {t1.size} B ({'placeholder' if t1.size < 1024 else 'a real TOC1?'})") |
| `h713/extract.py:149` | self.log.warn(f"{MIPS_FEX} fehlt im Image — keine MIPS-/Display-Artefakte aus diesem Eingang") | self.log.warn(f"{MIPS_FEX} is missing from the image -- no MIPS/display artefacts out of this input") |
| `h713/extract.py:153` | self.mips_source(br, f"{MIPS_FEX} im Image (= bootloader_a und bootloader_b)", | self.mips_source(br, f"{MIPS_FEX} in the image (= bootloader_a and bootloader_b)", |
| `h713/extract.py:157` | self.log.warn("super.fex fehlt im Image — kein EDID/MSP/PQ aus diesem Eingang") | self.log.warn("super.fex is missing from the image -- no EDID/MSP/PQ out of this input") |
| `h713/extract.py:172` | self.input_facts["typ"] = "eMMC-Dump" self.log.heading("Roher eMMC-Dump") gpt = Gpt(q, self.log) self.input_facts["gpt"] = {n: {"start_lba": s, "sektoren": c} for n, (s, c) in gpt.parts.items()} | self.input_facts["type"] = "eMMC dump" self.log.heading("raw eMMC dump") gpt = Gpt(q, self.log) self.input_facts["gpt"] = {n: {"start_lba": s, "sectors": c} for n, (s, c) in gpt.parts.items()} |
| `h713/extract.py:179` | self.log.info(f"LBA {lba}: eGON.BT0 (boot0/SPL) — Länge {struct.unpack_from('<I', b, 16)[0]} B") else: self.log.info(f"LBA {lba}: kein eGON.BT0 ({hexdump_short(b, 12)})") | self.log.info(f"LBA {lba}: eGON.BT0 (boot0/SPL) -- length {struct.unpack_from('<I', b, 16)[0]} B") else: self.log.info(f"LBA {lba}: no eGON.BT0 ({hexdump_short(b, 12)})") |
| `h713/extract.py:188` | self.log.info("Suche nach weiteren 'sunxi-package'-Köpfen im Dump …") for off in find_sunxi_packages(q, self.log): if all(off != k[0] for k in candidates): candidates.append((off, f"Suche @{off:#x} (LBA {off // SECTOR})")) if not candidates: self.log.warn("kein sunxi-package im Dump — kein scp.bin aus diesem Eingang") for off, where in candidates: try: self.package_from_source(q, f"Dump {where}", off) | self.log.info("searching the dump for further 'sunxi-package' headers ...") for off in find_sunxi_packages(q, self.log): if all(off != k[0] for k in candidates): candidates.append((off, f"search @{off:#x} (LBA {off // SECTOR})")) if not candidates: self.log.warn("no sunxi-package in the dump -- no scp.bin out of this input") for off, where in candidates: try: self.package_from_source(q, f"dump {where}", off) |
| `h713/extract.py:203` | self.log.warn(f"{n} liegt {'bei LBA %d+%d, ' % s if s else ''}nicht (vollständig) im Dump") | self.log.warn(f"{n} is {'at LBA %d+%d, but ' % s if s else ''}not (fully) in the dump") |
| `h713/extract.py:206` | self.mips_source(p, f"Dump: Partition {n} (LBA {gpt.parts[n][0]}, {gpt.parts[n][1]} Sektoren)", (n,)) else: self.log.warn(f"{n}: kein FAT-Bootsektor ({hexdump_short(p.read(0, 8))}) — MIPS-Dateien nicht lesbar") | self.mips_source(p, f"dump: partition {n} (LBA {gpt.parts[n][0]}, {gpt.parts[n][1]} sectors)", (n,)) else: self.log.warn(f"{n}: no FAT boot sector ({hexdump_short(p.read(0, 8))}) -- MIPS files not readable") |
| `h713/extract.py:211` | self.log.info(f"private@{s0}+{c0}: Secure Storage (HDCP-Keys laut doku/68) — wird nicht gelesen") self.observations["hdcp_hinweise_gpt"] = [f"private@{s0}+{c0} (Secure Storage, nicht gelesen)"] | self.log.info(f"private@{s0}+{c0}: secure storage (HDCP keys, doku/68) -- not read") self.observations["hdcp_notes_gpt"] = [f"private@{s0}+{c0} (secure storage, not read)"] |
| `h713/extract.py:216` | self.log.warn(f"'super' liegt {'bei LBA %d+%d, ' % s if s else ''}nicht (vollständig) im Dump — " f"kein EDID/MSP/PQ aus diesem Eingang") self.not_extracted.append("super nicht im Dump (EDID, MSP-Patch, PQ)") | self.log.warn(f"'super' is {'at LBA %d+%d, but ' % s if s else ''}not (fully) in the dump -- " f"no EDID/MSP/PQ out of this input") self.not_extracted.append("super not in the dump (EDID, MSP patch, PQ)") |
| `h713/extract.py:223` | self.input_facts["typ"] = "Einzelteile" self.log.heading("Einzelne Partitions-/.fex-Dateien") | self.input_facts["type"] = "single parts" self.log.heading("single partition/.fex files") |
| `h713/extract.py:232` | self.log.warn(f"{d / n} fehlt") | self.log.warn(f"{d / n} is missing") |
| `h713/extract.py:234` | raise Abort("keine Eingänge (--part … / --fex-dir …)") | raise Abort("no inputs (--part ... / --fex-dir ...)") |
| `h713/extract.py:235` | self.input_facts["teile"] = {} for k, v in parts.items(): pv = Path(v) if not pv.is_file(): raise Abort(f"--part {k}: Datei fehlt: {v}") h = None if self.args.no_hash else sha256_file(pv) self.input_facts["teile"][k] = {"pfad": str(pv), "groesse": pv.stat().st_size, "sha256": h} | self.input_facts["parts"] = {} for k, v in parts.items(): pv = Path(v) if not pv.is_file(): raise Abort(f"--part {k}: file missing: {v}") h = None if self.args.no_hash else sha256_file(pv) self.input_facts["parts"][k] = {"path": str(pv), "size": pv.stat().st_size, "sha256": h} |
| `h713/extract.py:257` | self.log.warn(f"{k}: kein sunxi-package gefunden") | self.log.warn(f"{k}: no sunxi-package found") |
| `h713/extract.py:277` | raise Abort("emmc: keine GPT bei LBA 1") | raise Abort("emmc: no GPT at LBA 1") |
| `h713/extract.py:287` | raise Abort(f"{origin}: kein sunxi-package") | raise Abort(f"{origin}: no sunxi-package") |
| `h713/extract.py:296` | self.input_facts["sunxi_package"] = {"herkunft": origin, "offset": off, "items": hashes, "valid_len": pk.valid_len, "pruefsumme_ok": pk.checksum_ok, "probleme": pk.problems} # Features for recognition and comparison self.features["paket_items"] = {n: l for n, (o, l) in pk.items.items()} self.features["paket_item_sha256"] = {n: h["sha256"] for n, h in hashes.items()} | self.input_facts["sunxi_package"] = {"origin": origin, "offset": off, "items": hashes, "valid_len": pk.valid_len, "checksum_ok": pk.checksum_ok, "problems": pk.problems} # Features for recognition and comparison self.features["package_items"] = {n: l for n, (o, l) in pk.items.items()} self.features["package_item_sha256"] = {n: h["sha256"] for n, h in hashes.items()} |
| `h713/extract.py:309` | self.log.info(f"U-Boot-Kennung: {k}") | self.log.info(f"U-Boot identifier: {k}") |
| `h713/extract.py:316` | self.log.info(f"dtb-Wurzel: model '{w.get('model', '')}', compatible '{w.get('compatible', '')}'") | self.log.info(f"dtb root: model '{w.get('model', '')}', compatible '{w.get('compatible', '')}'") |
| `h713/extract.py:320` | self.log.heading("h713-arisc.bin (= scp aus dem sunxi-package)") if not self.packages: self.log.warn("kein sunxi-package — h713-arisc.bin nicht extrahiert") self.not_extracted.append("h713-arisc.bin (kein sunxi-package im Eingang)") | self.log.heading("h713-arisc.bin (= scp out of the sunxi-package)") if not self.packages: self.log.warn("no sunxi-package -- h713-arisc.bin not extracted") self.not_extracted.append("h713-arisc.bin (no sunxi-package in the input)") |
| `h713/extract.py:330` | self.log.warn(f"{pk.origin}: kein 'scp'-Item (Items: {', '.join(pk.items)})") | self.log.warn(f"{pk.origin}: no 'scp' item (items: {', '.join(pk.items)})") |
| `h713/extract.py:336` | self.not_extracted.append("h713-arisc.bin (kein scp-Item)") | self.not_extracted.append("h713-arisc.bin (no scp item)") |
| `h713/extract.py:339` | self.log.warn("die gefundenen scp-Blobs unterscheiden sich: " + "; ".join(f"{h[:12]}… aus {', '.join(w)}" for h, w in seen.items())) else: h, where = next(iter(seen.items())) self.log.info(f"scp identisch in: {', '.join(where)}") | self.log.warn("the scp blobs found differ: " + "; ".join(f"{h[:12]}... from {', '.join(w)}" for h, w in seen.items())) else: h, where = next(iter(seen.items())) self.log.info(f"scp identical in: {', '.join(where)}") |
| `h713/extract.py:347` | self.log.info("Struktur: " + b) if b.startswith("Versionsstring: '"): self.features["arisc_version"] = b[len("Versionsstring: '"):].rstrip("'") if "arisc_version" in self.features: self.detect_device("ARISC-Versionsstring") checks = ["sunxi-package Prüfsumme " + ("ok" if pk.checksum_ok else "FALSCH")] + [ "Item-Tabelle ok" if not pk.problems else "Paketprobleme: " + "; ".join(pk.problems)] + findings error = (not pk.checksum_ok) or bool(pk.problems) or any("kein l.j" in b or "nicht 'CPUs'" in b or "leer" in b for b in findings) self.store("lib/firmware/h713-arisc.bin", scp, origin=f"{pk.origin}: Item 'scp' @{pk.off + pk.items['scp'][0]:#x}, {len(scp)} B " f"(Paket: {', '.join(f'{n}={l}' for n, (o, l) in pk.items.items())})", checks=checks, error=error) | self.log.info("structure: " + b) if b.startswith("version string: '"): self.features["arisc_version"] = b[len("version string: '"):].rstrip("'") if "arisc_version" in self.features: self.detect_device("ARISC version string") checks = ["sunxi-package checksum " + ("ok" if pk.checksum_ok else "WRONG")] + [ "item table ok" if not pk.problems else "package problems: " + "; ".join(pk.problems)] + findings error = (not pk.checksum_ok) or bool(pk.problems) or \ any("is not an l.j" in b or "not 'CPUs'" in b or "empty" in b for b in findings) self.store("lib/firmware/h713-arisc.bin", scp, origin=f"{pk.origin}: item 'scp' @{pk.off + pk.items['scp'][0]:#x}, {len(scp)} B " f"(package: {', '.join(f'{n}={l}' for n, (o, l) in pk.items.items())})", checks=checks, error=error) |
| `h713/extract.py:364` | self.log.heading("super → LP-Metadaten → vendor") | self.log.heading("super -> LP metadata -> vendor") |
| `h713/extract.py:371` | self.log.info(f"{q.name}: kein Sparse-Kopf, wird als rohes Abbild gelesen") | self.log.info(f"{q.name}: no sparse header, read as a raw image") |
| `h713/extract.py:374` | self.input_facts["lp"] = {"version": lp.version, "lage": lp.location, "partitionen": {n: {"size": p["size"], "gruppe": p["gruppe"], "extents": [list(e) for e in p["extents"]]} for n, p in lp.parts.items()}} | self.input_facts["lp"] = {"version": lp.version, "location": lp.location, "partitions": {n: {"size": p["size"], "group": p["group"], "extents": [list(e) for e in p["extents"]]} for n, p in lp.parts.items()}} |
| `h713/extract.py:380` | self.log.info(f"vendor-Partition: {n} ({vq.size} B)") | self.log.info(f"vendor partition: {n} ({vq.size} B)") |
| `h713/extract.py:384` | self.detect_device("LP-Partition " + n) return self.log.warn("keine Partition vendor_a/vendor in den LP-Metadaten: " + ", ".join(lp.parts)) self.not_extracted.append("vendor-Partition nicht gefunden (EDID, MSP-Patch, PQ)") | self.detect_device("LP partition " + n) return self.log.warn("no partition vendor_a/vendor in the LP metadata: " + ", ".join(lp.parts)) self.not_extracted.append("vendor partition not found (EDID, MSP patch, PQ)") |
| `h713/extract.py:394` | self.log.heading("vendor-Dateisystem") | self.log.heading("vendor file system") |
| `h713/extract.py:419` | if not self.open_vendor(): self.log.warn("keine vendor-Partition — hy310-edid.bin nicht extrahiert") self.not_extracted.append("hy310-edid.bin (keine vendor-Partition)") | if not self.open_vendor(): self.log.warn("no vendor partition -- hy310-edid.bin not extracted") self.not_extracted.append("hy310-edid.bin (no vendor partition)") |
| `h713/extract.py:427` | self.log.warn(f"{pf} fehlt in vendor") | self.log.warn(f"{pf} is missing from vendor") |
| `h713/extract.py:433` | checks.append(f"{Path(pf).name}: {len(b)} B, Hersteller {edid_vendor(b)}, Name '{edid_name(b)}', " f"Byte 168 {b[168]:#04x}" + (", Blockprüfsummen 0" if not problems else "")) | checks.append(f"{Path(pf).name}: {len(b)} B, vendor {edid_vendor(b)}, name '{edid_name(b)}', " f"byte 168 {b[168]:#04x}" + (", block checksums 0" if not problems else "")) |
| `h713/extract.py:441` | self.not_extracted.append("hy310-edid.bin (EDID-Dateien fehlen)") | self.not_extracted.append("hy310-edid.bin (EDID files missing)") |
| `h713/extract.py:445` | checks.append(f"Gesamtlänge {len(edid)} statt 512") | checks.append(f"total length {len(edid)} instead of 512") |
| `h713/extract.py:448` | origin=f"vendor:{EDID_14} ({len(parts[0])} B) + vendor:{EDID_20} ({len(parts[1])} B), unverändert aneinandergehängt", | origin=f"vendor:{EDID_14} ({len(parts[0])} B) + vendor:{EDID_20} ({len(parts[1])} B), concatenated unchanged", |
| `h713/extract.py:452` | self.log.heading("h713/msp-patch.bin (= Symbol patch_msp aus libmspsound.so)") | self.log.heading("h713/msp-patch.bin (= symbol patch_msp out of libmspsound.so)") |
| `h713/extract.py:454` | self.log.warn("keine vendor-Partition — msp-patch.bin nicht extrahiert") self.not_extracted.append("msp-patch.bin (keine vendor-Partition)") | self.log.warn("no vendor partition -- msp-patch.bin not extracted") self.not_extracted.append("msp-patch.bin (no vendor partition)") |
| `h713/extract.py:468` | self.log.warn("libmspsound.so nicht in vendor gefunden — msp-patch.bin nicht extrahiert") self.not_extracted.append("msp-patch.bin (libmspsound.so fehlt)") | self.log.warn("libmspsound.so not found in vendor -- msp-patch.bin not extracted") self.not_extracted.append("msp-patch.bin (libmspsound.so missing)") |
| `h713/extract.py:477` | self.log.warn("Symbol patch_msp nicht in der Symboltabelle — Rückfall auf MSPM-Kettensuche") chain = find_mspm_chain(data) if chain is None: self.not_extracted.append("msp-patch.bin (weder Symbol noch MSPM-Kette)") return off, size = chain[0], chain[1] - chain[0] checks.append(f"Herkunft per MSPM-Suche @{off:#x}, {size} B (kein Symbol!)") error = True else: off, size, sect = sym checks.append(f"Symbol patch_msp: Dateioffset {off:#x}, st_size {size}, Sektion {sect}") | self.log.warn("symbol patch_msp not in the symbol table -- falling back to the MSPM chain search") chain = find_mspm_chain(data) if chain is None: self.not_extracted.append("msp-patch.bin (neither a symbol nor an MSPM chain)") return off, size = chain[0], chain[1] - chain[0] checks.append(f"origin found by MSPM search @{off:#x}, {size} B (no symbol!)") error = True else: off, size, sect = sym checks.append(f"symbol patch_msp: file offset {off:#x}, st_size {size}, section {sect}") |
| `h713/extract.py:492` | checks.append(f"MSPM: {len(blocks)} Blöcke, {pairs} Paare, Ziele " + "/".join(f"{b['ziel']}:{b['laenge']}" for b in blocks)) | checks.append(f"MSPM: {len(blocks)} blocks, {pairs} pairs, targets " + "/".join(f"{b['target']}:{b['length']}" for b in blocks)) |
| `h713/extract.py:498` | checks.append(f"Länge {len(blob)} nicht durch 4 teilbar (Treiber verlangt das)") | checks.append(f"length {len(blob)} is not divisible by 4 (the driver requires that)") |
| `h713/extract.py:501` | origin=f"vendor:{lib}, Symbol patch_msp (Dateioffset {off:#x}, {size} B)", | origin=f"vendor:{lib}, symbol patch_msp (file offset {off:#x}, {size} B)", |
| `h713/extract.py:505` | self.log.heading("PQ-Quellen (die Dateien, die h713-pq liest)") | self.log.heading("PQ sources (the files h713-pq reads)") |
| `h713/extract.py:507` | self.log.warn("keine vendor-Partition — PQ nicht extrahiert") self.not_extracted.append("PQ-Dateien (keine vendor-Partition)") return present = {e["name"]: e for e in self.vendor.ls(TVCONFIG)} self.observations["tvconfig_inhalt"] = sorted(f"{n} ({e['size']} B)" for n, e in present.items()) for n in PQ_FILES: if n not in present: self.log.warn(f"{TVCONFIG}/{n} fehlt") self.not_extracted.append(f"pq/{n} (fehlt in vendor)") | self.log.warn("no vendor partition -- PQ not extracted") self.not_extracted.append("PQ files (no vendor partition)") return present = {e["name"]: e for e in self.vendor.ls(TVCONFIG)} self.observations["tvconfig_contents"] = sorted(f"{n} ({e['size']} B)" for n, e in present.items()) for n in PQ_FILES: if n not in present: self.log.warn(f"{TVCONFIG}/{n} is missing") self.not_extracted.append(f"pq/{n} (missing from vendor)") |
| `h713/extract.py:522` | checks=(["Pflichtschlüssel ok"] if not problems else problems), error=bool(problems)) | checks=(["mandatory keys ok"] if not problems else problems), error=bool(problems)) |
| `h713/extract.py:533` | self.log.heading(f"WLAN-Firmware (= vendor:{AIC_FW_DIR}/)") | self.log.heading(f"WLAN firmware (= vendor:{AIC_FW_DIR}/)") |
| `h713/extract.py:535` | self.log.warn("keine vendor-Partition — WLAN-Firmware nicht extrahiert") self.not_extracted.append("WLAN-Firmware (keine vendor-Partition)") return if not self.vendor.exists(AIC_FW_DIR): self.log.info(f"{AIC_FW_DIR} nicht vorhanden — dieses Geraet hat keine AIC8800-Firmware") self.observations["aic8800_firmware"] = "nicht vorhanden" return files = [e for e in self.vendor.ls(AIC_FW_DIR) if e.get("typ") != "d"] if not files: self.log.warn(f"{AIC_FW_DIR} ist leer") self.not_extracted.append("WLAN-Firmware (Verzeichnis leer)") self.observations["aic8800_firmware"] = "Verzeichnis leer" return | self.log.warn("no vendor partition -- WLAN firmware not extracted") self.not_extracted.append("WLAN firmware (no vendor partition)") return if not self.vendor.exists(AIC_FW_DIR): self.log.info(f"{AIC_FW_DIR} not present -- this device has no AIC8800 firmware") self.observations["aic8800_firmware"] = "not present" return files = [e for e in self.vendor.ls(AIC_FW_DIR) if e.get("typ") != "d"] if not files: self.log.warn(f"{AIC_FW_DIR} is empty") self.not_extracted.append("WLAN firmware (directory empty)") self.observations["aic8800_firmware"] = "directory empty" return |
| `h713/extract.py:554` | problems.append(f"gelesen {len(b)} B, Verzeichniseintrag sagt {e['size']} B") if not b: problems.append("leere Datei") self.store(f"{AIC_FW_TARGET}/{e['name']}", b, origin=f"vendor:{AIC_FW_DIR}/{e['name']}, unveraendert uebernommen", checks=problems or ["unveraendert uebernommen"], | problems.append(f"read {len(b)} B, the directory entry says {e['size']} B") if not b: problems.append("empty file") self.store(f"{AIC_FW_TARGET}/{e['name']}", b, origin=f"vendor:{AIC_FW_DIR}/{e['name']}, taken over unchanged", checks=problems or ["taken over unchanged"], |
| `h713/extract.py:567` | self.log.info(f"MIPS-Quelle gemerkt: {origin} ({q.size} B)") | self.log.info(f"MIPS source noted: {origin} ({q.size} B)") |
| `h713/extract.py:576` | entries = fs.directory(MIPS_SOURCE_DIR) if entries is None: self.log.warn(f"{origin}: kein Verzeichnis '{MIPS_SOURCE_DIR}/' im {fs.type} " f"(Wurzel: {', '.join(sorted(e['name'] for e in fs.entries(0))) or 'leer'})") return None files: Dict[str, bytes] = {} problems: List[str] = list(fs.problems) leftover: List[str] = [] short_names: Dict[str, str] = {} for e in sorted(entries, key=lambda x: x["name"]): if e["verzeichnis"]: leftover.append(f"{MIPS_SOURCE_DIR}/{e['name']}/ (Verzeichnis)") continue | entries = fs.directory(MIPS_SOURCE_DIR) if entries is None: self.log.warn(f"{origin}: no directory '{MIPS_SOURCE_DIR}/' in the {fs.type} " f"(root: {', '.join(sorted(e['name'] for e in fs.entries(0))) or 'empty'})") return None files: Dict[str, bytes] = {} problems: List[str] = list(fs.problems) leftover: List[str] = [] short_names: Dict[str, str] = {} for e in sorted(entries, key=lambda x: x["name"]): if e["verzeichnis"]: leftover.append(f"{MIPS_SOURCE_DIR}/{e['name']}/ (directory)") continue |
| `h713/extract.py:599` | short_names[e["name"]] = e["kurz"] if not e["lang"]: problems.append(f"{e['name']}: kein Langname im Verzeichnis — 8.3-Name genommen") | short_names[e["name"]] = e["kurz"] if not e["lang"]: problems.append(f"{e['name']}: no long name in the directory -- the 8.3 name was taken") |
| `h713/extract.py:607` | others.append(f"{e['name']}{'/' if e['verzeichnis'] else ''} ({'Verzeichnis' if e['verzeichnis'] else str(e['groesse']) + ' B'})") return {"herkunft": origin, "fs": fs.description, "typ": fs.type, "art": "fat", "dateien": files, "kurznamen": short_names, "probleme": problems, "uebrig": leftover, "sonst": others} | others.append(f"{e['name']}{'/' if e['verzeichnis'] else ''} ({'directory' if e['verzeichnis'] else str(e['groesse']) + ' B'})") return {"origin": origin, "fs": fs.description, "type": fs.type, "kind": "fat", "files": files, "short_names": short_names, "problems": problems, "leftover": leftover, "others": others} |
| `h713/extract.py:614` | Same read set as _read_mips(); the vendor filesystem has no 8.3 names, "kurznamen" stays empty.""" | Same read set as _read_mips(); the vendor filesystem has no 8.3 names, "short_names" stays empty.""" |
| `h713/extract.py:629` | return {"herkunft": VENDOR_MIPS_SOURCE + "/", "fs": self.vendor.description, "typ": "ext4", "art": "vendor", "dateien": files, "kurznamen": {}, "probleme": problems, "uebrig": leftover, "sonst": []} | return {"origin": VENDOR_MIPS_SOURCE + "/", "fs": self.vendor.description, "type": "ext4", "kind": "vendor", "files": files, "short_names": {}, "problems": problems, "leftover": leftover, "others": []} |
| `h713/extract.py:654` | r = reader() notes.append({"source": key, "origin": origin, "files": len(r["dateien"]) if r else None, | r = reader() notes.append({"source": key, "origin": origin, "files": len(r["files"]) if r else None, |
| `h713/extract.py:616` | if self.vendor is None and not self.open_vendor(): return {"id": None, "quelle": None, "hinweis": "keine vendor-Partition im Eingang — nicht ermittelbar"} except Abort as e: return {"id": None, "quelle": None, "hinweis": f"vendor-Dateisystem nicht lesbar ({e})"} | if self.vendor is None and not self.open_vendor(): return {"id": None, "source": None, "note": "no vendor partition in the input -- cannot be determined"} except Abort as e: return {"id": None, "source": None, "note": f"vendor file system not readable ({e})"} |
| `h713/extract.py:685` | return {"id": pid, "quelle": f"vendor:{pf}", "hinweis": None if pid is not None else "ProjectID-Zeile nicht gefunden"} return {"id": None, "quelle": None, "hinweis": "panel_config.ini nicht in vendor gefunden (" + ", ".join(PANEL_CONFIG_CANDIDATES) + ")"} | return {"id": pid, "source": f"vendor:{pf}", "note": None if pid is not None else "no ProjectID line found"} return {"id": None, "source": None, "note": "panel_config.ini not found in vendor (" + ", ".join(PANEL_CONFIG_CANDIDATES) + ")"} |
| `h713/extract.py:700` | main_set = ([r for r in read_sets if all(n in r["dateien"] for n in MIPS_FILES)] or [r for r in read_sets if "display.bin" in r["dateien"]] or read_sets)[0] for note in source_notes: if note["role"] is None: note["role"] = "used" if note["origin"] == main_set["herkunft"] else "cross-check" | main_set = ([r for r in read_sets if all(n in r["files"] for n in MIPS_FILES)] or [r for r in read_sets if "display.bin" in r["files"]] or read_sets)[0] for note in source_notes: if note["role"] is None: note["role"] = "used" if note["origin"] == main_set["origin"] else "cross-check" |
| `h713/extract.py:708` | a, b = main_set["dateien"], w["dateien"] difference = ([f"only in {main_set['herkunft']}: {n}" for n in sorted(set(a) - set(b))] + [f"only in {w['herkunft']}: {n}" for n in sorted(set(b) - set(a))] + | a, b = main_set["files"], w["files"] difference = ([f"only in {main_set['origin']}: {n}" for n in sorted(set(a) - set(b))] + [f"only in {w['origin']}: {n}" for n in sorted(set(b) - set(a))] + |
| `h713/extract.py:715` | comparison.append(f"{w['herkunft']} differs from {main_set['herkunft']}: " + "; ".join(difference)) self.log.warn(comparison[-1]) else: comparison.append(f"{w['herkunft']} is byte-identical to {main_set['herkunft']} ({len(b)} files)") self.log.info(comparison[-1]) files = main_set["dateien"] | comparison.append(f"{w['origin']} differs from {main_set['origin']}: " + "; ".join(difference)) self.log.warn(comparison[-1]) else: comparison.append(f"{w['origin']} is byte-identical to {main_set['origin']} ({len(b)} files)") self.log.info(comparison[-1]) files = main_set["files"] |
| `h713/extract.py:726` | self.log.info(f"Quelle: {main_set['herkunft']} — {main_set['fs']}") self.log.info(f"mips/: {len(files)} Dateien für unsere Kette, {len(main_set['uebrig'])} weitere darin") for n, k in sorted(main_set["kurznamen"].items()): if k.lower() != n.lower(): self.log.info(f" Langname '{n}' (8.3 wäre '{k}')") | self.log.info(f"source: {main_set['origin']} -- {main_set['fs']}") self.log.info(f"mips/: {len(files)} file(s) for our chain, {len(main_set['leftover'])} more in there") for n, k in sorted(main_set["short_names"].items()): if k.lower() != n.lower(): self.log.info(f" long name '{n}' (8.3 would be '{k}')") |
| `h713/extract.py:746` | self.log.warn("display.bin fehlt in mips/ — die benutzte Projekt-ID ist damit nicht bestimmbar") | self.log.warn("display.bin is missing from mips/ -- the used project id cannot be determined") |
| `h713/extract.py:764` | display_checks.append(f"h713_mips_fw_revs[]: {rev['board']}, Projekt {rev['project_id']:#04x}, " f"Panel {rev['panel']}, Sollgröße {rev['size']} B") self.log.info(f"display.bin: bekannt — {rev['board']}, Projekt-ID {rev['project_id']:#04x}, Panel {rev['panel']}") | display_checks.append(f"h713_mips_fw_revs[]: {rev['board']}, project {rev['project_id']:#04x}, " f"panel {rev['panel']}, expected size {rev['size']} B") self.log.info(f"display.bin: known -- {rev['board']}, project id {rev['project_id']:#04x}, panel {rev['panel']}") |
| `h713/extract.py:771` | note = (f"display.bin dieses {DEVICES[self.device]['name']} ist byteidentisch mit der Revision, die in " f"h713_mips_fw_revs[] unter '{rev['board']}' steht — der Name dort bezeichnet die Herkunft der " f"Revision, nicht das Gerät. Projekt-ID {rev['project_id']:#04x} gilt trotzdem.") | note = (f"the display.bin of this {DEVICES[self.device]['name']} is byte-identical to the revision " f"h713_mips_fw_revs[] lists under '{rev['board']}' -- the name there says where the revision " f"came from, not which device this is. Project id {rev['project_id']:#04x} holds all the same.") |
| `h713/extract.py:777` | display_checks.append(f"Größe {len(db)} B statt {rev['size']} B (Revisionstabelle)") self.log.warn(display_checks[-1]) display_error = True else: display_checks.append(f"Größe {len(db)} B = Revisionstabelle") else: display_error = True display_checks.append(f"sha256 {sha256_bytes(db)} steht NICHT in h713_mips_fw_revs[] — " f"Projekt-ID unbekannt") self.log.warn(f"display.bin ({len(db)} B, sha256 {sha256_bytes(db)}) ist keine der bekannten Revisionen " f"({', '.join(r['board'] + ' ' + hex(r['project_id']) for r in UBOOT_FW_REVS)}) — " f"die benutzte Projekt-ID lässt sich nicht bestimmen") self.log.warn("Ausweg: alle ProjectID-Dateien liegen in der Ausgabe; die richtige zur Laufzeit wählen " "(setenv h713_project 0x…; saveenv), Plan 108 §4.5 drittens") | display_checks.append(f"size {len(db)} B instead of {rev['size']} B (revision table)") self.log.warn(display_checks[-1]) display_error = True else: display_checks.append(f"size {len(db)} B = revision table") else: display_error = True display_checks.append(f"sha256 {sha256_bytes(db)} is NOT in h713_mips_fw_revs[] -- " f"project id unknown") self.log.warn(f"display.bin ({len(db)} B, sha256 {sha256_bytes(db)}) is none of the known revisions " f"({', '.join(r['board'] + ' ' + hex(r['project_id']) for r in UBOOT_FW_REVS)}) -- " f"the used project id cannot be determined") self.log.warn("way out: every ProjectID file is in the output; pick the right one at runtime " "(setenv h713_project 0x...; saveenv), plan 108 section 4.5 thirdly") |
| `h713/extract.py:806` | declared = self.declared_project_id() if declared["id"] is not None: self.log.info(f"deklarierte Projekt-ID: {declared['id']:#04x} ({declared['id']} dezimal) aus {declared['quelle']} — " f"wird gemeldet, nicht benutzt (Plan 108 §4.5)") else: self.log.info(f"deklarierte Projekt-ID: {declared['hinweis']}") used = rev["project_id"] if rev else None if used is not None and declared["id"] is not None and used != declared["id"]: self.log.warn(f"benutzte Projekt-ID {used:#04x} und deklarierte {declared['id']:#04x} gehen auseinander — " f"benutzt wird {used:#04x} (aus display.bin), Plan 108 §3/§6") if used is not None and f"0x{used:04x}" not in found_ids: self.log.warn(f"ProjectID_0x{used:04x}.TSE fehlt in mips/, obwohl display.bin genau diese ID verlangt") | declared = self.declared_project_id() if declared["id"] is not None: self.log.info(f"declared project id: {declared['id']:#04x} ({declared['id']} decimal) from {declared['source']} -- " f"reported, not used (plan 108 section 4.5)") else: self.log.info(f"declared project id: {declared['note']}") used = rev["project_id"] if rev else None if used is not None and declared["id"] is not None and used != declared["id"]: self.log.warn(f"the used project id {used:#04x} and the declared one {declared['id']:#04x} disagree -- " f"{used:#04x} (from display.bin) is what counts, plan 108 section 3/6") if used is not None and f"0x{used:04x}" not in found_ids: self.log.warn(f"ProjectID_0x{used:04x}.TSE is missing from mips/, although display.bin asks for exactly that id") |
| `h713/extract.py:819` | self.mips = { "quelle": main_set["herkunft"], "dateisystem": main_set["fs"], | self.mips = { "source": main_set["origin"], "filesystem": main_set["fs"], |
| `h713/extract.py:828` | "projekt_id_benutzt": f"{used:#04x}" if used is not None else None, "projekt_id_benutzt_woher": (f"sha256 der display.bin in h713_mips_fw_revs[] -> {rev['board']}, " f"Panel {rev['panel']}" if rev and rev["project_id"] is not None else f"undetermined: the revision is '{rev['board']}', which no row of " f"h713_mips_fw_revs[] declares" if rev else "unbestimmbar: display.bin steht nicht in h713_mips_fw_revs[]"), "projekt_id_deklariert": f"{declared['id']:#04x}" if declared["id"] is not None else None, "projekt_id_deklariert_quelle": declared["quelle"], "projekt_id_deklariert_hinweis": declared["hinweis"], "projekt_id_deklariert_benutzt": False, "projekt_id_dateien": found_ids, "display_bin_bekannt": bool(rev), "revisionstabelle": [f"{r['board']}: {r['project_id']:#04x}, Panel {r['panel']}, {r['size']} B, " f"sha256 {r['sha256'][:16]}…" for r in UBOOT_FW_REVS], "nicht_extrahiert_gleiche_partition": main_set["sonst"] + main_set["uebrig"], } self.observations["bootloader_partition_rest"] = main_set["sonst"] + main_set["uebrig"] if main_set["sonst"] or main_set["uebrig"]: self.log.info("in derselben Partition, von unserer Kette ungenutzt (wird nicht kopiert, Plan 108 §1): " + ", ".join(main_set["sonst"] + main_set["uebrig"])) | "project_id_used": f"{used:#04x}" if used is not None else None, "project_id_used_from": (f"sha256 of the display.bin in h713_mips_fw_revs[] -> {rev['board']}, " f"panel {rev['panel']}" if rev and rev["project_id"] is not None else f"undetermined: the revision is '{rev['board']}', which no row of " f"h713_mips_fw_revs[] declares" if rev else "undetermined: display.bin is not in h713_mips_fw_revs[]"), "project_id_declared": f"{declared['id']:#04x}" if declared["id"] is not None else None, "project_id_declared_source": declared["source"], "project_id_declared_note": declared["note"], "project_id_declared_used": False, "project_id_files": found_ids, "display_bin_known": bool(rev), "revision_table": [f"{r['board']}: {r['project_id']:#04x}, panel {r['panel']}, {r['size']} B, " f"sha256 {r['sha256'][:16]}..." for r in UBOOT_FW_REVS], "not_extracted_same_partition": main_set["others"] + main_set["leftover"], } self.observations["bootloader_partition_remainder"] = main_set["others"] + main_set["leftover"] if main_set["others"] or main_set["leftover"]: self.log.info("in the same partition, unused by our chain (not copied, plan 108 section 1): " + ", ".join(main_set["others"] + main_set["leftover"])) |
| `h713/extract.py:850` | fat = main_set["art"] == "fat" for n in sorted(files, key=lambda x: (bool(MIPS_PROJECTID.match(x)), x)): d = files[n] # The vendor copy has no 8.3 short names -- its own line says where the file came from. checks: List[str] = [f"aus {main_set['herkunft']}, {MIPS_SOURCE_DIR}/{n} " f"(8.3-Kurzname '{main_set['kurznamen'].get(n, '?')}')"] if fat else \ [f"from {main_set['herkunft']}{n} ({len(d)} B, vendor copy)"] | fat = main_set["kind"] == "fat" for n in sorted(files, key=lambda x: (bool(MIPS_PROJECTID.match(x)), x)): d = files[n] # The vendor copy has no 8.3 short names -- its own line says where the file came from. checks: List[str] = [f"from {main_set['origin']}, {MIPS_SOURCE_DIR}/{n} " f"(8.3 short name '{main_set['short_names'].get(n, '?')}')"] if fat else \ [f"from {main_set['origin']}{n} ({len(d)} B, vendor copy)"] |
| `h713/extract.py:864` | error = any("nicht parsebar" in x for x in p) | error = any("not parsable" in x for x in p) |
| `h713/extract.py:870` | error = any("Magic fehlt" in x or "passt nicht zusammen" in x for x in p) for x in p: if "Magic fehlt" in x or "passt nicht zusammen" in x: self.log.warn(x) for x in main_set["probleme"]: | error = any("magic missing" in x or "do not match" in x for x in p) for x in p: if "magic missing" in x or "do not match" in x: self.log.warn(x) for x in main_set["problems"]: |
| `h713/extract.py:878` | origin=f"{main_set['herkunft']}: {MIPS_SOURCE_DIR}/{n} ({len(d)} B, FAT-Langname)" if fat else f"{main_set['herkunft']}{n} ({len(d)} B, vendor copy inside super)", checks=checks, error=error) for n in MIPS_FILES: if n not in files: self.log.warn(f"{MIPS_SOURCE_DIR}/{n} fehlt in der Quelle") self.not_extracted.append(f"{MIPS_OUTPUT_DIR}/{n} (fehlt in {main_set['herkunft']})") if not found_ids: self.log.warn("keine einzige ProjectID_0x*.TSE in mips/ gefunden") self.not_extracted.append(f"{MIPS_OUTPUT_DIR}/ProjectID_0x*.TSE (keine gefunden)") | origin=f"{main_set['origin']}: {MIPS_SOURCE_DIR}/{n} ({len(d)} B, FAT long name)" if fat else f"{main_set['origin']}{n} ({len(d)} B, vendor copy inside super)", checks=checks, error=error) for n in MIPS_FILES: if n not in files: self.log.warn(f"{MIPS_SOURCE_DIR}/{n} is missing from the source") self.not_extracted.append(f"{MIPS_OUTPUT_DIR}/{n} (missing from {main_set['origin']})") if not found_ids: self.log.warn("not a single ProjectID_0x*.TSE found in mips/") self.not_extracted.append(f"{MIPS_OUTPUT_DIR}/ProjectID_0x*.TSE (none found)") |
| `h713/extract.py:891` | self.log.heading("Beobachtungen (nichts davon wird extrahiert)") if not self.open_vendor(): self.log.info("keine vendor-Partition — keine Beobachtungen möglich") | self.log.heading("observations (none of this is extracted)") if not self.open_vendor(): self.log.info("no vendor partition -- no observations possible") |
| `h713/extract.py:911` | modules.append({"pfad": pf, "groesse": e["size"], "modinfo": infos}) | modules.append({"path": pf, "size": e["size"], "modinfo": infos}) |
| `h713/extract.py:930` | notice = {"eintraege": len(files), "aic_eintraege": aic_entries} | notice = {"entries": len(files), "aic_entries": aic_entries} |
| `h713/extract.py:932` | notice = {"fehler": str(e)} self.observations["notice_xml"] = notice self.observations["aic8800_dateien"] = sorted(aic) self.observations["aic8800_module"] = modules self.observations["aic8800_firmware_strings"] = fw_strings self.observations["hdcp_hinweise"] = sorted(hdcp) self.observations["lizenzdateien"] = sorted(licence) if notice is not None: self.log.info(f"/etc/NOTICE.xml.gz: {notice.get('eintraege')} Dateieinträge, davon zu aic8800: " f"{len(notice.get('aic_eintraege', []))} — {'keine Lizenzangabe zur AIC-Firmware' if not notice.get('aic_eintraege') else notice['aic_eintraege']}") self.log.info(f"aic8800-bezogene Dateien: {len(aic)}") for line in sorted(aic): self.log.info(" " + line) for m in modules: self.log.info(f" Modul {m['pfad']}: " + "; ".join(m["modinfo"])) for pf, found in fw_strings.items(): self.log.info(f" Strings in {pf}: " + " \| ".join(found)) self.log.info(f"HDCP-Hinweise (Dateinamen, nicht angefasst): {len(hdcp)}") for line in sorted(hdcp): self.log.info(" " + line) self.log.info(f"Lizenz-/Notice-Dateien: {len(licence)}") for line in sorted(licence)[:20]: self.log.info(" " + line) if isinstance(self.vendor, Ext4): self.log.info("ext4-Leser: " + self.vendor.stats()) | notice = {"error": str(e)} self.observations["notice_xml"] = notice self.observations["aic8800_files"] = sorted(aic) self.observations["aic8800_modules"] = modules self.observations["aic8800_firmware_strings"] = fw_strings self.observations["hdcp_notes"] = sorted(hdcp) self.observations["licence_files"] = sorted(licence) if notice is not None: self.log.info(f"/etc/NOTICE.xml.gz: {notice.get('entries')} file entries, of them about aic8800: " f"{len(notice.get('aic_entries', []))} -- {'no licence statement for the AIC firmware' if not notice.get('aic_entries') else notice['aic_entries']}") self.log.info(f"aic8800-related files: {len(aic)}") for line in sorted(aic): self.log.info(" " + line) for m in modules: self.log.info(f" module {m['path']}: " + "; ".join(m["modinfo"])) for pf, found in fw_strings.items(): self.log.info(f" strings in {pf}: " + " \| ".join(found)) self.log.info(f"HDCP notes (file names, never touched): {len(hdcp)}") for line in sorted(hdcp): self.log.info(" " + line) self.log.info(f"licence/notice files: {len(licence)}") for line in sorted(licence)[:20]: self.log.info(" " + line) if isinstance(self.vendor, Ext4): self.log.info("ext4 reader: " + self.vendor.stats()) |
| `h713/extract.py:970` | self.log.warn(f"{mp}: vorhandenes Manifest unlesbar ({e}) — wird überschrieben") return g = old.get("device") or (old.get("image_typ") or "").lower() or None # image_typ: manifests of version 0.1 self.old_device = g if g in DEVICES else None self.log.info(f"{mp}: vorhandenes Manifest ({old.get('werkzeug', '?')}, {old.get('zeit', '?')}) für Gerät " f"{self.old_device or 'unbekannt/keins'}") | self.log.warn(f"{mp}: the manifest already there is unreadable ({e}) -- it will be overwritten") return g = old.get("device") or (old.get("image_typ") or "").lower() or None # image_typ: manifests of version 0.1 self.old_device = g if g in DEVICES else None self.log.info(f"{mp}: a manifest is already there ({old.get('tool') or old.get('werkzeug', '?')}, " f"{old.get('time') or old.get('zeit', '?')}) for device {self.old_device or 'unknown/none'}") |
| `h713/extract.py:980` | raise Abort(f"Ausgabeverzeichnis {self.out} enthält schon Ergebnisse für Gerät '{self.old_device}', dieser Eingang " f"ist '{self.device}' — Ergebnisse zweier Geräte werden nicht vermischt. Anderes --out wählen " f"(nichts wurde überschrieben).") | raise Abort(f"the output directory {self.out} already holds results for device '{self.old_device}', this input " f"is '{self.device}' -- results of two devices are not mixed. Choose another --out " f"(nothing was overwritten).") |
| `h713/extract.py:989` | self.detection = {"ueber": via if gid else None, "merkmale": features or [], "hinweise": self.detection.get("hinweise", [])} if gid: self.log.info(f"GERÄT: {gid} — {DEVICES[gid]['beschreibung']} (erkannt über {via}" + (": " + ", ".join(features) if features else "") + ")") | self.detection = {"via": via if gid else None, "features": features or [], "notes": self.detection.get("notes", [])} if gid: self.log.info(f"DEVICE: {gid} -- {description_of(gid)} (recognised by {via}" + (": " + ", ".join(features) if features else "") + ")") |
| `h713/extract.py:1007` | self.detection["stimmen"] = {g: {"treffer": t, "widerspruch": w} for g, (t, w) in votes.items()} | self.detection["votes"] = {g: {"hits": t, "against": w} for g, (t, w) in votes.items()} |
| `h713/extract.py:1010` | if fitting[0] == self.device: self.detection["merkmale"] = votes[fitting[0]][0] # write down later confirmations as well else: self.set_device(fitting[0], "Inhalt", features=votes[fitting[0]][0]) | if fitting[0] == self.device: self.detection["features"] = votes[fitting[0]][0] # write down later confirmations as well else: self.set_device(fitting[0], "content", features=votes[fitting[0]][0]) |
| `h713/extract.py:1017` | seen = self.detection.setdefault("_gesehen", []) | seen = self.detection.setdefault("_seen", []) |
| `h713/extract.py:1024` | note = (f"Merkmale widersprüchlich für {g}: passt bei {', '.join(t)}, nicht bei " f"{', '.join(f'{m}={self.features[m]!r}' for m in w)}") self.detection["hinweise"].append(note) self.log.warn(note + f" (nach {reason})") if self.device is not None: self.log.warn(f"Gerät {self.device} wieder zurückgenommen — Eingangsteile passen nicht zu einem Gerät") self.set_device(None, reason) elif len(fitting) > 1: self.log.warn(f"Merkmale passen zu mehreren Profilen ({', '.join(fitting)}) — Gerät bleibt unbekannt") | note = (f"features contradictory for {g}: they fit at {', '.join(t)}, not at " f"{', '.join(f'{m}={self.features[m]!r}' for m in w)}") self.detection["notes"].append(note) self.log.warn(note + f" (after {reason})") if self.device is not None: self.log.warn(f"device {self.device} withdrawn again -- the parts of the input do not fit one device") self.set_device(None, reason) elif len(fitting) > 1: self.log.warn(f"the features fit several profiles ({', '.join(fitting)}) -- the device stays unknown") |
| `h713/extract.py:60` | profile = PROFILES.get(board_id) if board_id else None return (profile or {}).get("status") | profile = PROFILES.get(board_id) if board_id else None return (profile or {}).get("status") def description_of(board_id: Optional[str]) -> Optional[str]: """The English one-liner of a board profile -- stage 3 replaces the German LEGACY_DESCRIPTIONS that `legacy_devices()` still carries (h713/profiles is not this package's to change).""" profile = PROFILES.get(board_id) if board_id else None if profile and profile.get("description"): return profile["description"] return DEVICES[board_id]["beschreibung"] if board_id in DEVICES else None |
| `h713/extract.py:1046` | e, nm, m, out = DEVICES[gid]["erwartung"], DEVICES[gid]["name"], self.features, [] if "paket_items" in m: for n, expected in e["paket_items"].items(): if n not in m["paket_items"]: out.append(f"Paket-Item {n} fehlt ({nm} hat es)") elif m["paket_items"][n] != expected: out.append(f"Paket-Item {n}: {m['paket_items'][n]} B statt {expected} B ({nm})") for n, sh in e["paket_item_sha256"].items(): actual = m["paket_item_sha256"].get(n) if actual and actual != sh: out.append(f"Paket-Item {n}: Inhalt anders als {nm} (sha256 {actual[:16]}…)") if "uboot_version" in m and not feature_matches("uboot_version", m["uboot_version"], e["uboot_version"]): out.append(f"U-Boot-Kennung '{m['uboot_version']}' statt '{e['uboot_version']} …' ({nm})") if "dtb_compatible" in m and e["dtb_compatible"] not in m["dtb_compatible"]: out.append(f"dtb compatible '{m['dtb_compatible']}' ohne '{e['dtb_compatible']}' — andere SoC-Familie?") if "arisc_version" in m and not feature_matches("arisc_version", m["arisc_version"], e["arisc_version"]): out.append(f"ARISC-Versionsstring '{m['arisc_version']}' statt '{e['arisc_version']} …' ({nm})") if "vendor_size" in m and m["vendor_size"] != e["vendor_size"]: out.append(f"vendor-Partition {m['vendor_size']} B statt {e['vendor_size']} B ({nm})") if "libmspsound_sha256" in m and m["libmspsound_sha256"] != e["libmspsound_sha256"]: out.append(f"libmspsound.so anders als {nm} (sha256 {m['libmspsound_sha256'][:16]}…) — Patchstrom prüfen") if "build_fingerprint" in m and m["build_fingerprint"] != e["build_fingerprint"]: out.append(f"Vendor-Fingerprint '{m['build_fingerprint']}' statt '{e['build_fingerprint']}' ({nm})") if "mips_database_sha256" in m and m["mips_database_sha256"] != e["mips_database_sha256"]: out.append(f"mips/database.TSE anders als {nm} (sha256 {m['mips_database_sha256'][:16]}…) — " f"andere Vendor-Displaydaten") if "sunxi_version" in m and m["sunxi_version"] != e["sunxi_version"]: out.append(f"sunxi_version {m['sunxi_version']} statt {e['sunxi_version']} ({nm})") return out | e, nm, m, out = DEVICES[gid]["erwartung"], DEVICES[gid]["name"], self.features, [] if "package_items" in m: for n, expected in e["paket_items"].items(): if n not in m["package_items"]: out.append(f"package item {n} is missing ({nm} has it)") elif m["package_items"][n] != expected: out.append(f"package item {n}: {m['package_items'][n]} B instead of {expected} B ({nm})") for n, sh in e["paket_item_sha256"].items(): actual = m["package_item_sha256"].get(n) if actual and actual != sh: out.append(f"package item {n}: content differs from {nm} (sha256 {actual[:16]}...)") if "uboot_version" in m and not feature_matches("uboot_version", m["uboot_version"], e["uboot_version"]): out.append(f"U-Boot identifier '{m['uboot_version']}' instead of '{e['uboot_version']} ...' ({nm})") if "dtb_compatible" in m and e["dtb_compatible"] not in m["dtb_compatible"]: out.append(f"dtb compatible '{m['dtb_compatible']}' without '{e['dtb_compatible']}' -- another SoC family?") if "arisc_version" in m and not feature_matches("arisc_version", m["arisc_version"], e["arisc_version"]): out.append(f"ARISC version string '{m['arisc_version']}' instead of '{e['arisc_version']} ...' ({nm})") if "vendor_size" in m and m["vendor_size"] != e["vendor_size"]: out.append(f"vendor partition {m['vendor_size']} B instead of {e['vendor_size']} B ({nm})") if "libmspsound_sha256" in m and m["libmspsound_sha256"] != e["libmspsound_sha256"]: out.append(f"libmspsound.so differs from {nm} (sha256 {m['libmspsound_sha256'][:16]}...) -- check the patch stream") if "build_fingerprint" in m and m["build_fingerprint"] != e["build_fingerprint"]: out.append(f"vendor fingerprint '{m['build_fingerprint']}' instead of '{e['build_fingerprint']}' ({nm})") if "mips_database_sha256" in m and m["mips_database_sha256"] != e["mips_database_sha256"]: out.append(f"mips/database.TSE differs from {nm} (sha256 {m['mips_database_sha256'][:16]}...) -- " f"other vendor display data") if "sunxi_version" in m and m["sunxi_version"] != e["sunxi_version"]: out.append(f"sunxi_version {m['sunxi_version']} instead of {e['sunxi_version']} ({nm})") return out |
| `h713/extract.py:1078` | self.log.heading("Gerät und Referenzabgleich") if self.device: self.reference_device = self.device if self.device_fixed: # recognised by sha256: which content features confirm that, for the manifest k = features_of(DEVICES[self.device]) self.detection["merkmale"] = [m for m in ID_FEATURES if m in self.features and feature_matches(m, self.features[m], k[m])] self.log.info(f"Gerät: {self.device} [profile status: {profile_status(self.device)}] — " f"{DEVICES[self.device]['beschreibung']}; erkannt über {self.detection['ueber']}" + (f" ({'bestätigt durch' if self.device_fixed else 'Merkmale'}: " + ", ".join(self.detection["merkmale"]) + ")" if self.detection["merkmale"] else "")) else: self.reference_device = self.closest_profile() self.log.warn(f"UNBEKANNTES IMAGE — kein Geräteprofil passt (bekannt: {', '.join(DEVICES)}). Best-Effort: Abgleich " f"gegen das ähnlichste Profil '{self.reference_device}'; jede Abweichung unten ist ein Warnzeichen") | self.log.heading("device and reference comparison") if self.device: self.reference_device = self.device if self.device_fixed: # recognised by sha256: which content features confirm that, for the manifest k = features_of(DEVICES[self.device]) self.detection["features"] = [m for m in ID_FEATURES if m in self.features and feature_matches(m, self.features[m], k[m])] self.log.info(f"device: {self.device} [profile status: {profile_status(self.device)}] -- " f"{description_of(self.device)}; recognised by {self.detection['via']}" + (f" ({'confirmed by' if self.device_fixed else 'features'}: " + ", ".join(self.detection["features"]) + ")" if self.detection["features"] else "")) else: self.reference_device = self.closest_profile() self.log.warn(f"UNKNOWN IMAGE -- no device profile fits (known: {', '.join(DEVICES)}). Best effort: compared " f"against the closest profile '{self.reference_device}'; every deviation below is a warning sign") |
| `h713/extract.py:1098` | a["referenzgeraet"] = self.reference_device a["referenz_stimmt"] = (a["groesse"] == ref[0] and a["sha256"] == ref[1]) if a["referenz_stimmt"]: a["pruefung"].append(f"Referenz {self.reference_device} stimmt ({ref[1][:16]}…)") self.log.info(f"{a['pfad']}: Referenz {self.reference_device} stimmt") else: a["pruefung"].append(f"Referenz {self.reference_device} weicht ab: erwartet {ref[0]} B {ref[1][:16]}…, " f"ist {a['groesse']} B {a['sha256'][:16]}…") (self.log.warn if self.device else self.log.info)( f"{a['pfad']}: weicht von der Referenz {self.reference_device} ab" + ("!" if self.device else " (unbekanntes Image — erwartbar)")) | a["reference_device"] = self.reference_device a["reference_ok"] = (a["size"] == ref[0] and a["sha256"] == ref[1]) if a["reference_ok"]: a["checks"].append(f"reference {self.reference_device} matches ({ref[1][:16]}...)") self.log.info(f"{a['path']}: reference {self.reference_device} matches") else: a["checks"].append(f"reference {self.reference_device} deviates: expected {ref[0]} B {ref[1][:16]}..., " f"got {a['size']} B {a['sha256'][:16]}...") (self.log.warn if self.device else self.log.info)( f"{a['path']}: deviates from the reference {self.reference_device}" + ("!" if self.device else " (unknown image -- to be expected)")) |
| `h713/extract.py:1095` | ref = profile["referenz"].get(a["pfad"]) | ref = profile["referenz"].get(a["path"]) |
| `h713/extract.py:1079` | if self.device: self.log.warn(f"Eingang weicht vom {profile['name']}-Stock ab, obwohl das Gerät erkannt wurde — Profil oder Eingang prüfen") self.log.heading(f"Abweichungen vom {profile['name']}-Stock" + (" — hier drohen Probleme" if not self.device else "")) for a in self.deviations: self.log.info("- " + a) if self.old_device and not self.device: self.log.warn(f"{self.out} enthielt ein Manifest für Gerät '{self.old_device}'; dieser Lauf ist unbekannt — " f"Verzeichnisinhalt ist jetzt gemischt, besser trennen") errors = [a for a in self.artefacts if a["fehler"]] ref_no = [a for a in self.artefacts if a["referenz_stimmt"] is False] missing = [p for p in REQUIRED_FILES if not any(a["pfad"] == p for a in self.artefacts)] | if self.device: self.log.warn(f"the input deviates from the {profile['name']} stock although the device was recognised " f"-- check the profile or the input") self.log.heading(f"deviations from the {profile['name']} stock" + (" -- trouble ahead" if not self.device else "")) for a in self.deviations: self.log.info("- " + a) if self.old_device and not self.device: self.log.warn(f"{self.out} held a manifest for device '{self.old_device}'; this run is unknown -- " f"the directory content is mixed now, better keep them apart") errors = [a for a in self.artefacts if a["error"]] ref_no = [a for a in self.artefacts if a["reference_ok"] is False] missing = [p for p in REQUIRED_FILES if not any(a["path"] == p for a in self.artefacts)] |
| `h713/extract.py:1146` | self.artefacts.append({"pfad": rel, "groesse": len(data), "sha256": h, "md5": hashlib.md5(data).hexdigest(), "herkunft": origin, "pruefung": checks, "fehler": error, "referenz_stimmt": None, "referenzgeraet": None}) state = "FEHLER" if error else "ok" self.log.info(f"-> {rel}: {len(data)} B, sha256 {h}, Prüfung {state}") | self.artefacts.append({"path": rel, "size": len(data), "sha256": h, "md5": hashlib.md5(data).hexdigest(), "origin": origin, "checks": checks, "error": error, "reference_ok": None, "reference_device": None}) state = "ERROR" if error else "ok" self.log.info(f"-> {rel}: {len(data)} B, sha256 {h}, check {state}") |
| `h713/extract.py:1160` | q = self.open(self.args.input) self.fingerprint(q, "Eingang") | q = self.open(self.args.input) self.fingerprint(q, "input") |
| `h713/extract.py:1167` | self.input_facts["typ"] = "sunxi-package" self.package_from_source(q, q.path.name) elif SparseSource.is_sparse(q) or q.read(4096, 4) == b"gDla": self.input_facts["typ"] = "super" self.super_from_source(q) elif q.size > 0x800 and struct.unpack_from("<H", q.read(0x438, 2), 0)[0] == 0xEF53: self.input_facts["typ"] = "ext4 (vendor)" self.vendor_source = q elif Fat.is_fat(q): self.input_facts["typ"] = "FAT (bootloader_a/_b bzw. boot-resource.fex)" self.mips_source(q, f"{q.path.name} (FAT-Abbild)", ("bootloader_b", "bootloader_a")) else: raise Abort(f"Eingang nicht erkannt: {q.path} ({hexdump_short(q.read(0, 16))}) — " f"weder IMAGEWTY, GPT-Dump, sunxi-package, Sparse/LP-super, ext4 noch FAT") | self.input_facts["type"] = "sunxi-package" self.package_from_source(q, q.path.name) elif SparseSource.is_sparse(q) or q.read(4096, 4) == b"gDla": self.input_facts["type"] = "super" self.super_from_source(q) elif q.size > 0x800 and struct.unpack_from("<H", q.read(0x438, 2), 0)[0] == 0xEF53: self.input_facts["type"] = "ext4 (vendor)" self.vendor_source = q elif Fat.is_fat(q): self.input_facts["type"] = "FAT (bootloader_a/_b or boot-resource.fex)" self.mips_source(q, f"{q.path.name} (FAT image)", ("bootloader_b", "bootloader_a")) else: raise Abort(f"input not recognised: {q.path} ({hexdump_short(q.read(0, 16))}) -- " f"neither IMAGEWTY, GPT dump, sunxi-package, sparse/LP super, ext4 nor FAT") |
| `h713/extract.py:1196` | self.log.heading("Ergebnis: Exit 2 — Ausgabeverzeichnis gehört einem anderen Gerät, nichts geschrieben") | self.log.heading("result: exit 2 -- the output directory belongs to another device, nothing written") |
| `h713/extract.py:1213` | self.log.info(f"Zwischenstände bleiben (--keep-tmp): {self.tmp}") | self.log.info(f"scratch files kept (--keep-tmp): {self.tmp}") |
| `h713/extract.py:1218` | self.log.info(f"Zwischenstände gelöscht ({size} B in {self.tmp})") | self.log.info(f"scratch files deleted ({size} B in {self.tmp})") |
| `h713/vendorfiles.py:163` | blocks.append({"offset": o, "ziel": dsp, "laenge": length, "paare": length // 4}) | blocks.append({"offset": o, "target": dsp, "length": length, "pairs": length // 4}) |
| `h713/vendorfiles.py:326` | return {"magic_ok": False, "id": None, "kopf": data.hex()} | return {"magic_ok": False, "id": None, "header": data.hex()} |
| `h713/vendorfiles.py:329` | "kopf": data[:16].hex()} | "header": data[:16].hex()} |
| `h713/vendorfiles.py:337` | findings.append(f"{name}: TSE magic missing (header {k['kopf']})") | findings.append(f"{name}: TSE magic missing (header {k['header']})") |
| `h713/vendorfiles.py:345` | findings.append(f"{name}: TSE header {k['kopf']}, ID {k['id']:#06x} = file name") | findings.append(f"{name}: TSE header {k['header']}, ID {k['id']:#06x} = file name") |
| `h713/vendorfiles.py:347` | findings.append(f"{name}: TSE header {k['kopf']}, ID field {k['id']:#06x}") | findings.append(f"{name}: TSE header {k['header']}, ID field {k['id']:#06x}") |
| `h713/extract.py:491` | pairs = sum(b["paare"] for b in blocks) | pairs = sum(b["pairs"] for b in blocks) |
| `h713/extract.py:493` | "/".join(f"{b['ziel']}:{b['laenge']}" for b in blocks)) | "/".join(f"{b['target']}:{b['length']}" for b in blocks)) |
| `h713/fs/lpsuper.py:101` | "gruppe": groups[grp] if grp < len(groups) else "?"} | "group": groups[grp] if grp < len(groups) else "?"} |
| `h713/fs/lpsuper.py:109` | log.info(f" {name:20s} {p['size']:12d} B attrs {p['attrs']:#x} Gruppe {p['gruppe']} [{ex or 'leer'}]") | log.info(f" {name:20s} {p['size']:12d} B attrs {p['attrs']:#x} group {p['group']} [{ex or 'empty'}]") |
| `h713/extract.py:375` | "partitions": {n: {"size": p["size"], "group": p["gruppe"], | "partitions": {n: {"size": p["size"], "group": p["group"], |
| `h713/vendorfiles.py:231` | """Empty list = ok. Otherwise findings („hier drohen Probleme").""" | """Empty list = ok. Otherwise findings (each one is trouble ahead).""" |
| `h713/extract.py:691` | f"{MIPS_OUTPUT_DIR}/… (MIPS/display artefacts from the bootloader FAT and the vendor copy)" | f"{MIPS_OUTPUT_DIR}/... (MIPS/display artefacts from the bootloader FAT and the vendor copy)" |
| `h713/extract.py:711` | [f"{n} differs: {len(a[n])} B sha256 {sha256_bytes(a[n])[:12]}… against " f"{len(b[n])} B sha256 {sha256_bytes(b[n])[:12]}…" | [f"{n} differs: {len(a[n])} B sha256 {sha256_bytes(a[n])[:12]}... against " f"{len(b[n])} B sha256 {sha256_bytes(b[n])[:12]}..." |
| `h713/mkimage.py:6` | plus the offset table, a manifest, checksums and a readme file; `check` validates a finished image against its table. The trees for the two ext4 file systems come from `tree_boot`/`tree_rootfs` -- what is in them and where it goes is `h713.layout`. Moved from hy310-mkimage.py (M:408-1152), stage 1 (doku/121). The CLI (`main()`, the argument parser and the tool docstring shown as its epilog) stays in installer/hy310-mkimage.py. """ | plus the offset table, a manifest, checksums and a README file; `check` validates a finished image against its table. The trees for the two ext4 file systems come from `tree_boot`/`tree_rootfs` -- what is in them and where it goes is `h713.layout`. Moved from hy310-mkimage.py (M:408-1152), stage 1 (doku/121); stage 3 made every printed line and the README English and renamed the README file from <stem>-LIESMICH.txt to <stem>-README.txt. The table keys are unchanged (format string "hy310-abbild-tabelle" included) until stage 4. The CLI (`main()`, the argument parser and the tool docstring shown as its epilog) stays in installer/h713-mkimage. """ |
| `h713/mkimage.py:47` | # Version of the hy310-mkimage tool (M:70), not the version of the package # (h713.VERSION): it goes into the table as "werkzeug" and the installer reads # tables that were written with it. | # Version of the h713-mkimage tool (M:70), not the version of the package # (h713.VERSION): it goes into the table as "werkzeug" and the installer reads # tables that were written with it. |
| `h713/mkimage.py:81` | raise SystemExit("%s traegt keine FIT-Kennung d00dfeed" % fit) | raise SystemExit("%s carries no FIT identifier d00dfeed" % fit) |
| `h713/mkimage.py:82` | log.ok("%-34s %9d Byte (echt)" % (KERNEL_FIT, os.path.getsize(target))) | log.ok("%-34s %9d bytes (real)" % (KERNEL_FIT, os.path.getsize(target))) |
| `h713/mkimage.py:92` | log.ok("%d Dateien in %s" % (n, directory)) | log.ok("%d files in %s" % (n, directory)) |
| `h713/mkimage.py:124` | log.ok("%d Platzhalter in %s (davon %d vom Nutzer zu fuellen: %s)" % (n, directory, len(USER_PLACEHOLDERS), | log.ok("%d placeholders in %s (%d of them for the user to fill: %s)" % (n, directory, len(USER_PLACEHOLDERS), |
| `h713/mkimage.py:108` | # The user placeholders: modes set explicitly, not left to the umask. # The owner is whoever writes the tree -- so that has to be root # (mkimage-eingaben.sh checks it); step 5 checks it in the ext4 afterwards. | # The user placeholders: modes set explicitly, not left to the umask. # The owner is whoever writes the tree -- so that has to be root # (mkimage-inputs.sh checks it); step 5 checks it in the ext4 afterwards. |
| `h713/mkimage.py:150` | raise SystemExit("%s: %s gibt es nicht" % (image, path)) | raise SystemExit("%s: %s does not exist" % (image, path)) |
| `h713/mkimage.py:155` | raise SystemExit("%s: %s hat keine Datenbloecke" % (image, path)) | raise SystemExit("%s: %s has no data blocks" % (image, path)) |
| `h713/mkimage.py:158` | raise SystemExit("%s: %s beginnt mit einem Loch" % (image, path)) | raise SystemExit("%s: %s starts with a hole" % (image, path)) |
| `h713/mkimage.py:81` | raise SystemExit( "%s: %s liegt nicht am Stueck (%d Fragmente) -- ein " "einzelner Offset waere falsch" % (image, path, len(block_map))) | raise SystemExit( "%s: %s does not lie in one piece (%d fragments) -- a " "single offset would be wrong" % (image, path, len(block_map))) |
| `h713/mkimage.py:168` | raise SystemExit("%s: %s hat ein Loch am Ende" % (image, path)) | raise SystemExit("%s: %s has a hole at the end" % (image, path)) |
| `h713/mkimage.py:172` | raise SystemExit("%s: %s -- Gegenprobe am Offset %d schlug fehl" % (image, path, off)) | raise SystemExit("%s: %s -- the counter-check at offset %d failed" % (image, path, off)) |
| `h713/mkimage.py:175` | log.ok("%-22s Blockgroesse %d, %d Datei(en) am Stueck, Gegenprobe gleich" % (os.path.basename(image), bs, len(out))) | log.ok("%-22s block size %d, %d file(s) in one piece, counter-check equal" % (os.path.basename(image), bs, len(out))) |
| `h713/mkimage.py:197` | problems.append("%s fehlt" % path) | problems.append("%s is missing" % path) |
| `h713/mkimage.py:202` | problems.append("%s gehoert %d:%d statt %d:%d" % (path, i["uid"], i["gid"], uid, gid)) | problems.append("%s belongs to %d:%d instead of %d:%d" % (path, i["uid"], i["gid"], uid, gid)) |
| `h713/mkimage.py:204` | problems.append("%s hat Modus %04o statt %04o" % (path, actual, mode)) | problems.append("%s has mode %04o instead of %04o" % (path, actual, mode)) |
| `h713/mkimage.py:208` | raise SystemExit("%s: Besitz/Rechte stimmen nicht -- der Baum wurde nicht als " "root ausgepackt? (mkimage-eingaben.sh im Container mit " "`podman exec -u root` ausfuehren)" % os.path.basename(image)) log.ok("%-22s Besitz root:root und Modi geprueft (%d Pfade, darunter /root/.ssh 0700, authorized_keys 0600)" % (os.path.basename(image), len(expectation))) | raise SystemExit("%s: owner/permissions are wrong -- was the tree unpacked as " "root? (run mkimage-inputs.sh in the container with " "`podman exec -u root`)" % os.path.basename(image)) log.ok("%-22s owner root:root and the modes checked (%d paths, among them /root/.ssh 0700, authorized_keys 0600)" % (os.path.basename(image), len(expectation))) |
| `h713/mkimage.py:280` | raise SystemExit("%s: %s nicht gefunden" % (w, p)) | raise SystemExit("%s: %s not found" % (w, p)) |
| `h713/mkimage.py:293` | console.step(1, "Bausteine pruefen") | console.step(1, "check the building blocks") |
| `h713/mkimage.py:300` | console.warn("%s: keine Versionskennung in %s gefunden" % (what, path)) | console.warn("%s: no version identifier found in %s" % (what, path)) |
| `h713/mkimage.py:303` | raise SystemExit("%s traegt keine eGON.BT0-Kennung -- das ist keine SPL" % spl) | raise SystemExit("%s carries no eGON.BT0 identifier -- that is not an SPL" % spl) |
| `h713/mkimage.py:305` | raise SystemExit("SPL ist %d Byte, hy310-spl fasst %d" % (n_spl, 64 * SECTOR)) | raise SystemExit("the SPL is %d bytes, hy310-spl holds %d" % (n_spl, 64 * SECTOR)) |
| `h713/mkimage.py:307` | raise SystemExit("U-Boot ist %d Byte, hy310-uboot fasst %d" % (n_ub, 10240 * SECTOR)) | raise SystemExit("U-Boot is %d bytes, hy310-uboot holds %d" % (n_ub, 10240 * SECTOR)) |
| `h713/mkimage.py:309` | raise SystemExit("hy310-boot.ext4 ist %d Byte, die Partition hat %d" % (n_boot, 262144 * SECTOR)) | raise SystemExit("hy310-boot.ext4 is %d bytes, the partition has %d" % (n_boot, 262144 * SECTOR)) |
| `h713/mkimage.py:312` | raise SystemExit("hy310-rootfs.ext4 passt nicht (%d Byte)" % n_root) | raise SystemExit("hy310-rootfs.ext4 does not fit (%d bytes)" % n_root) |
| `h713/mkimage.py:323` | raise SystemExit("%s ist %d Byte, die Umgebung hat %d (CONFIG_ENV_SIZE)" % (env, n_env, ENV_BYTES)) | raise SystemExit("%s is %d bytes, the environment has %d (CONFIG_ENV_SIZE)" % (env, n_env, ENV_BYTES)) |
| `h713/mkimage.py:328` | raise SystemExit("%s: CRC32 im Kopf (%08x) passt nicht zum Inhalt -- keine U-Boot-Umgebung" % (env, env_crc)) | raise SystemExit("%s: the CRC32 in the header (%08x) does not fit the content -- not a U-Boot environment" % (env, env_crc)) |
| `h713/mkimage.py:332` | for w, p, n in (("SPL", spl, n_spl), ("U-Boot proper", ub, n_ub), ("Umgebung", env, n_env), ("hy310-boot.ext4", boot, n_boot), ("hy310-rootfs.ext4", root, n_root)): console.ok("%-16s %11d Byte %s" % (w, n, relpath_or_abs(p, project_root))) console.info(" Umgebung: %d Eintraege, CRC %08x, %s" % (len(env_entries), env_crc, ", ".join(env_gate) if env_gate else "KEIN h713_gate -- pruefen")) | for w, p, n in (("SPL", spl, n_spl), ("U-Boot proper", ub, n_ub), ("environment", env, n_env), ("hy310-boot.ext4", boot, n_boot), ("hy310-rootfs.ext4", root, n_root)): console.ok("%-16s %11d bytes %s" % (w, n, relpath_or_abs(p, project_root))) console.info(" environment: %d entries, CRC %08x, %s" % (len(env_entries), env_crc, ", ".join(env_gate) if env_gate else "NO h713_gate -- check it")) |
| `h713/mkimage.py:339` | console.step(2, "Teil A -- Boot-Kette (LBA %d..%d)" % (PART_A_LBA, PART_A_SECTORS - 1)) | console.step(2, "piece A -- boot chain (LBA %d..%d)" % (PART_A_LBA, PART_A_SECTORS - 1)) |
| `h713/mkimage.py:351` | console.ok("%s %d Byte (%.0f MiB)" % (os.path.basename(a_file), len(a), mib(len(a)))) | console.ok("%s %d bytes (%.0f MiB)" % (os.path.basename(a_file), len(a), mib(len(a)))) |
| `h713/mkimage.py:354` | console.step(3, "Teil C -- Sicherungskopie der GPT (LBA %d, %d Sektoren)" % (PART_C_LBA, PART_C_SECTORS)) | console.step(3, "piece C -- backup copy of the GPT (LBA %d, %d sectors)" % (PART_C_LBA, PART_C_SECTORS)) |
| `h713/mkimage.py:362` | console.ok("%s %d Byte -- raeumt zugleich Reste der alten Tabelle weg" % (os.path.basename(c_file), len(c))) | console.ok("%s %d bytes -- it also clears away leftovers of the old table" % (os.path.basename(c_file), len(c))) |
| `h713/mkimage.py:369` | raise SystemExit("die gebaute GPT ist nicht in Ordnung") console.ok("GPT nachgerechnet: CRCs, FirstUsable %d, %d Eintraege, sechs Partitionen, " "Sicherungskopie stimmt" % (FIRST_USABLE, GPT_ENTRY_COUNT)) | raise SystemExit("the GPT that was built is not in order") console.ok("GPT recomputed: CRCs, FirstUsable %d, %d entries, six partitions, " "backup copy matches" % (FIRST_USABLE, GPT_ENTRY_COUNT)) |
| `h713/mkimage.py:375` | console.step(4, "Teil B -- System (LBA %d ...)" % PART_B_LBA) | console.step(4, "piece B -- system (LBA %d ...)" % PART_B_LBA) |
| `h713/mkimage.py:384` | console.ok("%s %d Byte (%.0f MiB)" % (os.path.basename(b_file), n_b, mib(n_b))) console.info("hy310-env traegt die eingebaute Vorgabe des mitgelieferten U-Boot (%s) -- gueltig ab dem ersten Start" % (", ".join(env_gate) if env_gate else "ohne h713_gate")) | console.ok("%s %d bytes (%.0f MiB)" % (os.path.basename(b_file), n_b, mib(n_b))) console.info("hy310-env carries the built-in default of the U-Boot that ships with the image (%s) -- valid from the first start" % (", ".join(env_gate) if env_gate else "without h713_gate")) |
| `h713/mkimage.py:389` | console.step(5, "Platzhalter im Abbild finden") | console.step(5, "find the placeholders in the image") |
| `h713/mkimage.py:408` | raise SystemExit("%s: %d Byte im Dateisystem, %d in der Tabelle" % (name, g, size)) | raise SystemExit("%s: %d bytes in the file system, %d in the table" % (name, g, size)) |
| `h713/mkimage.py:416` | "quelle": source, "fuellung": "zeilenumbrueche" if is_user_placeholder(name) else "muster", | "quelle": source, "fuellung": "zeilenumbrueche" if is_user_placeholder(name) else "muster", # table keys/values: stage 4 |
| `h713/mkimage.py:420` | console.ok("%d Platzhalter fuer h713-extract, zusammen %d Byte" % (len(table), sum(g for _o, g in table.values()))) console.ok("%d Platzhalter fuer den Nutzer: %s" % (len(user), ", ".join("%s (%d Byte, Zeilenumbrueche)" % (n, g) for n, (_o, g) in user.items()))) | console.ok("%d placeholders for h713-extract, %d bytes in all" % (len(table), sum(g for _o, g in table.values()))) console.ok("%d placeholder(s) for the user: %s" % (len(user), ", ".join("%s (%d bytes, line breaks)" % (n, g) for n, (_o, g) in user.items()))) |
| `h713/mkimage.py:431` | raise SystemExit("%s: an Offset %d steht nicht die erwartete Fuellung" % (name, o)) console.ok("alle %d Offsets in Teil B gegengeprueft" % (len(table) + len(user))) | raise SystemExit("%s: the expected filling is not at offset %d" % (name, o)) console.ok("all %d offsets counter-checked in piece B" % (len(table) + len(user))) |
| `h713/mkimage.py:435` | console.step(6, "Der gesperrte Bereich") | console.step(6, "the locked range") |
| `h713/mkimage.py:440` | raise SystemExit("%s deckt LBA %d..%d ab und beruehrt damit den " "Secure Storage" % (file_name, lba, end)) console.ok("kein Teil beruehrt LBA %d..%d -- auch ein dd von Hand kann den Secure " "Storage nicht treffen" % (LOCK_FIRST, LOCK_LAST)) | raise SystemExit("%s covers LBA %d..%d and so touches the " "secure storage" % (file_name, lba, end)) console.ok("no piece touches LBA %d..%d -- not even a dd by hand can hit the secure " "storage" % (LOCK_FIRST, LOCK_LAST)) |
| `h713/mkimage.py:446` | console.step(7, "Tabelle, Manifest, Liesmich") | console.step(7, "table, manifest, README") |
| `h713/mkimage.py:519` | readme = stem + "-LIESMICH.txt" with open(readme, "w", encoding="utf-8") as f: f.write(readme_text(data)) console.ok(os.path.basename(readme)) total = sum(t["bytes"] for t in piece_list) console.step(8, "Fertig") console.info("drei Teile, zusammen %.0f MiB (%.2f GB)" % (mib(total), total / 1e9)) console.info("Verzeichnis: %s" % directory) console.info("Pruefen mit: %s --pruefen %s" % (os.path.basename(sys.argv[0]), relpath_or_abs(t_file, os.getcwd()))) return 0 | # doku/121 stage 3: the README is English and is called <stem>-README.txt; # <stem>-LIESMICH.txt is not written any more. readme = stem + "-README.txt" with open(readme, "w", encoding="utf-8") as f: f.write(readme_text(data)) console.ok(os.path.basename(readme)) total = sum(t["bytes"] for t in piece_list) console.step(8, "done") console.info("three pieces, %.0f MiB in all (%.2f GB)" % (mib(total), total / 1e9)) console.info("directory: %s" % directory) console.info("check with: %s check %s" % (os.path.basename(sys.argv[0]), relpath_or_abs(t_file, os.getcwd()))) return 0 |
| `h713/mkimage.py:542` | raise SystemExit("%s ist keine Abbild-Tabelle" % table_file) | raise SystemExit("%s is not an image table" % table_file) |
| `h713/mkimage.py:545` | log.step(1, "Teile: Groesse und Pruefsumme") | log.step(1, "pieces: size and checksum") |
| `h713/mkimage.py:550` | log.error("%s fehlt" % t["datei"]) | log.error("%s is missing" % t["datei"]) |
| `h713/mkimage.py:555` | log.error("%s ist %d Byte, erwartet %d" % (t["datei"], n, t["bytes"])) | log.error("%s is %d bytes, expected %d" % (t["datei"], n, t["bytes"])) |
| `h713/mkimage.py:560` | log.error("%s: sha256 %s…, erwartet %s…" % (t["datei"], h[:16], t["sha256"][:16])) | log.error("%s: sha256 %s..., expected %s..." % (t["datei"], h[:16], t["sha256"][:16])) |
| `h713/mkimage.py:564` | log.ok("%-34s LBA %-9d %11d Byte %s…" % (t["datei"], t["lba"], n, h[:16])) | log.ok("%-34s LBA %-9d %11d bytes %s..." % (t["datei"], t["lba"], n, h[:16])) |
| `h713/mkimage.py:566` | log.step(2, "Der gesperrte Bereich (Secure Storage)") | log.step(2, "the locked range (secure storage)") |
| `h713/mkimage.py:570` | log.error("die Tabelle nennt LBA %d..%d, dieses Werkzeug kennt %d..%d" % (first, last, LOCK_FIRST, LOCK_LAST)) | log.error("the table says LBA %d..%d, this tool knows %d..%d" % (first, last, LOCK_FIRST, LOCK_LAST)) |
| `h713/mkimage.py:577` | log.error("%s deckt LBA %d..%d ab und beruehrt den Secure Storage" % (name, lba, end)) | log.error("%s covers LBA %d..%d and touches the secure storage" % (name, lba, end)) |
| `h713/mkimage.py:582` | log.ok("kein Teil beruehrt LBA %d..%d -- der Bereich bleibt, wie er ist" % (first, last)) | log.ok("no piece touches LBA %d..%d -- the range stays as it is" % (first, last)) |
| `h713/mkimage.py:585` | log.step(3, "Partitionstabelle") | log.step(3, "partition table") |
| `h713/mkimage.py:589` | log.error("kein Teil bei LBA 0 -- GPT nicht pruefbar") | log.error("no piece at LBA 0 -- the GPT cannot be checked") |
| `h713/mkimage.py:599` | log.ok("CRCs stimmen, FirstUsableLBA %d, %d Eintraege, sechs Partitionen" % (FIRST_USABLE, GPT_ENTRY_COUNT)) log.ok("Sicherungskopie am Plattenende vorhanden und gleich (Befund S46 B7)") | log.ok("the CRCs match, FirstUsableLBA %d, %d entries, six partitions" % (FIRST_USABLE, GPT_ENTRY_COUNT)) log.ok("the backup copy at the end of the disk is there and equal (finding S46 B7)") |
| `h713/mkimage.py:603` | log.step(4, "Platzhalter") | log.step(4, "placeholders") |
| `h713/mkimage.py:606` | log.error("%s fehlt -- Platzhalter nicht pruefbar" % pd) | log.error("%s is missing -- the placeholders cannot be checked" % pd) |
| `h713/mkimage.py:617` | log.error("%s: Offset %d + %d liegt ausserhalb von %s" % (name, off, length, pd)) | log.error("%s: offset %d + %d lies outside %s" % (name, off, length, pd)) |
| `h713/mkimage.py:623` | log.error("%s: disk_offset in der Tabelle passt nicht zum Teil" % name) | log.error("%s: disk_offset in the table does not fit the piece" % name) |
| `h713/mkimage.py:628` | log.error("%s liegt im gesperrten Bereich" % name) | log.error("%s lies inside the locked range" % name) |
| `h713/mkimage.py:640` | log.ok("%d Platzhalter geprueft: %d unbefuellt (Fuellung steht), %d bereits gefuellt" % (len(both), unfilled, filled)) if broken: log.error("%d Platzhalter stimmen nicht" % broken) bad += broken if filled: log.warn("dieses Abbild ist nicht mehr das Verteilstueck -- es traegt " "geraeteeigene Daten und gehoert nicht weitergegeben") log.step(5, "Ergebnis") if bad: log.error("%d Beanstandung(en)" % bad) return 1 log.ok("alles in Ordnung") return 0 | log.ok("%d placeholders checked: %d unfilled (the filling is in place), %d already filled" % (len(both), unfilled, filled)) if broken: log.error("%d placeholders are wrong" % broken) bad += broken if filled: log.warn("this image is no longer the one to hand out -- it carries " "device-specific data and should not be passed on") log.step(5, "result") if bad: log.error("%d complaint(s)" % bad) return 1 log.ok("everything in order") return 0 |
| `tests/test_extract_mips.py:156` | read_set = {"herkunft": "vendor:/etc/display/mips/", "fs": "ext4 (synthetic)", "typ": "ext4", "art": "vendor", "dateien": files, "kurznamen": {}, "probleme": [], "uebrig": [], "sonst": []} run.mips_read_sets = lambda: ([read_set], [{"source": "vendor:/etc/display/mips", "origin": read_set["herkunft"], "files": len(files), "role": None}]) run.declared_project_id = lambda: {"id": None, "quelle": None, "hinweis": "synthetic input"} | # doku/121 stage 3 ("stage 3 texts"): the read-set keys of h713.extract are English -- # herkunft/typ/art/dateien/kurznamen/probleme/uebrig/sonst -> # origin/type/kind/files/short_names/problems/leftover/others, and # declared_project_id() returns id/source/note instead of id/quelle/hinweis. read_set = {"origin": "vendor:/etc/display/mips/", "fs": "ext4 (synthetic)", "type": "ext4", "kind": "vendor", "files": files, "short_names": {}, "problems": [], "leftover": [], "others": []} run.mips_read_sets = lambda: ([read_set], [{"source": "vendor:/etc/display/mips", "origin": read_set["origin"], "files": len(files), "role": None}]) run.declared_project_id = lambda: {"id": None, "source": None, "note": "synthetic input"} |
| `tests/test_extract_mips.py:177` | with open(os.path.join(out, "BERICHT.txt"), encoding="utf-8") as fh: report = fh.read() for line in ("UNKNOWN, the row a profile would need:", "name: unknown", | # stage 3: the report is REPORT.txt (BERICHT.txt is written as a copy for one release) with open(os.path.join(out, "REPORT.txt"), encoding="utf-8") as fh: report = fh.read() self.assertEqual(report, open(os.path.join(out, "BERICHT.txt"), encoding="utf-8").read()) for line in ("UNKNOWN, the row a profile would need:", "name: unknown", |
| `tests/test_extract_mips.py:204` | self.assertEqual(mips["quelle"], "vendor:/etc/display/mips/", text) | self.assertEqual(mips["source"], "vendor:/etc/display/mips/", text) |
| `tests/test_extract_mips.py:213` | got = sorted(a["pfad"] for a in man["artefakte"] if a["pfad"].startswith("boot/mips/")) | got = sorted(a["path"] for a in man["files"] if a["path"].startswith("boot/mips/")) |
| `tests/test_extract_mips.py:178` | with open(os.path.join(out, "BERICHT.txt"), encoding="utf-8") as fh: report = fh.read() self.assertIn("hdcp_wait_va: 0x4b13d1f0", report) | with open(os.path.join(out, "REPORT.txt"), encoding="utf-8") as fh: report = fh.read() self.assertIn("hdcp_wait_va: 0x4b13d1f0", report) |
| `tests/test_extract_mips.py:232` | self.assertTrue(mips["quelle"].startswith("boot-resource.fex"), text) | self.assertTrue(mips["source"].startswith("boot-resource.fex"), text) |
| `h713/fs/lpsuper.py:105` | f"LP-Metadaten {location} @{moff}: v{major}.{minor}, header_size {header_size}, {p_n} Partitionen, {e_n} Extents, " f"{g_n} Gruppen, Blockgeräte {', '.join(f'{n}({s} B)' for n, _f, s in devices)}") | f"LP metadata {location} @{moff}: v{major}.{minor}, header_size {header_size}, {p_n} partitions, {e_n} extents, " f"{g_n} groups, block devices {', '.join(f'{n}({s} B)' for n, _f, s in devices)}") |
| `h713/fs/lpsuper.py:108` | ex = "; ".join((f"linear Sektor {d}+{n}" if t == 0 else f"zero {n}") for n, t, d, _s in p["extents"]) | ex = "; ".join((f"linear sector {d}+{n}" if t == 0 else f"zero {n}") for n, t, d, _s in p["extents"]) |
| `h713/fs/lpsuper.py:99` | # dict keys stay as they are — the extractor's JSON depends on them (stage 1) self.parts[name] = {"attrs": attrs, "extents": exts, "size": size, | # stage 3: "gruppe" -> "group"; the extractor maps these keys into its JSON self.parts[name] = {"attrs": attrs, "extents": exts, "size": size, |
| `h713/mkimage.py:399` | [(n, g, t, p, "hy310-install --authorized-key") for n, g, t, p, _m in USER_PLACEHOLDERS] | [(n, g, t, p, "h713-install --ssh-key") for n, g, t, p, _m in USER_PLACEHOLDERS] |
| `h713/mkimage.py:496` | # Exactly the format that fill_placeholders() in hy310-install.py # expects: name -> (byte offset, length), offset inside the file named # under "platzhalter_datei". | # Exactly the format that fill_placeholders() in h713.install expects: # name -> (byte offset, length), offset inside the file named under # "platzhalter_datei". |
| `h713/mkimage.py:501` | # Same shape, different source: hy310-install fills these from # --authorized-key, padded with line breaks. An installer that does not # know about the key leaves the file empty (line breaks only) -- valid. | # Same shape, different source: h713-install fills these from --ssh-key, # padded with line breaks. An installer that does not know about the key # leaves the file empty (line breaks only) -- valid. |
| `h713/mkimage.py:730` | b("Put the device into FEL mode (hold the reset button, plug the power in),") b("then start the drive share with hy310-install. After that the eMMC is an") b("ordinary USB drive.") b("") b("The comfortable way -- hy310-install does dump, extraction and writing:") b("") b(" hy310-install --uboot u-boot-sunxi-with-spl.bin \\") b(" --abbild %s.tabelle.json" % d["abbild"]) | b("Put the device into FEL mode (hold the reset button, plug the power in),") b("then start the drive share with h713-install. After that the eMMC is an") b("ordinary USB drive.") b("") b("The comfortable way -- h713-install does dump, extraction and writing:") b("") b(" h713-install install %s.tabelle.json \\" % d["abbild"]) b(" --uboot u-boot-sunxi-with-spl.bin") |
| `h713/mkimage.py:764` | b("They come out of your own device: h713-extract reads them from the full") b("dump you pulled beforehand, and hy310-install writes them to the places") | b("They come out of your own device: h713-extract reads them from the full") b("dump you pulled beforehand, and h713-install writes them to the places") |
| `h713/mkimage.py:774` | b(" hy310-install ... --authorized-key ~/.ssh/id_ed25519.pub") | b(" h713-install install ... --ssh-key ~/.ssh/id_ed25519.pub") |
| `h713/mkimage.py:785` | b("plug the power in) and start over -- the boot chain is always reachable") b("from there. If you have a full dump, hy310-install --restore puts it back.") | b("plug the power in) and start over -- the boot chain is always reachable") b("from there. If you have a full dump, h713-install restore puts it back.") |
| `h713/mkimage.py:249` | mkimage-eingaben.sh puts tmp/); the module's own directory when not given.""" | mkimage-inputs.sh puts tmp/); the module's own directory when not given.""" |
| `h713/layout.py:9` | Moved from hy310-mkimage.py (M:77-204, M:261-293), stage 1 (doku/121). """ | Moved from hy310-mkimage.py (M:77-204, M:261-293), stage 1 (doku/121). Stage 3 changed no value here: the fill pattern of `pattern()` is image content, not a printed line -- see the note there. """ |
| `h713/layout.py:16` | # The locked range. Identical to LOCK_FIRST/LOCK_LAST in hy310-install.py -- | # The locked range. Identical to LOCK_FIRST/LOCK_LAST in h713.blockdev -- |
| `h713/layout.py:121` | # different filling. hy310-install lists them under "platzhalter_nutzer" in the | # different filling. h713-install lists them under "platzhalter_nutzer" in the |
| `h713/layout.py:135` | # was in image v0.5 on 11.09. (mkimage-eingaben.sh did not run as root). | # was in image v0.5 on 11.09. (mkimage-inputs.sh did not run as root). |
| `h713/layout.py:155` | Deliberately not a block of zeros: (1) in a hexdump you see at once that the file is not filled yet and which one it is, (2) for nothing but zeros mke2fs may create a hole (sparse) -- then there would be no physical blocks for the installer to write into. """ | Deliberately not a block of zeros: (1) in a hexdump you see at once that the file is not filled yet and which one it is, (2) for nothing but zeros mke2fs may create a hole (sparse) -- then there would be no physical blocks for the installer to write into. Stage 3 does NOT translate this string. It is not a printed line but the content of the image: it stands in every built image, its sha256 is in the table under "sha256_muster", and `h713-mkimage check` decides from it whether a placeholder is still unfilled. Translating it would make every image built before stage 3 read as "already filled". It goes when the release format changes (stage 4). """ |

Not in the table because they were written from scratch rather than replaced:
`installer/h713-mkimage` (new CLI, German docstring of `hy310-mkimage.py` translated),
`installer/h713-extract` (help texts), `installer/mkimage-selftest.py`,
`installer/mkimage-inputs.sh`, `h713/extract.py write_manifest()` (manifest + report),
`h713/mkimage.py BETA_WARNING` and `readme_text()` (the beta box keeps its content word for word).

## 6. Golden values re-frozen (reason: "stage 3 texts")

| test | value | old -> new |
|---|---|---|
| `test_mkimage_selftest.py` | `SELFTEST` | `support.SELFTEST_PY` -> `os.path.join(support.TOOLS, "mkimage-selftest.py")` |
| `test_mkimage_selftest.py` | `MARKERS` | `ALLES GRUEN`/`Secure Storage unveraendert`/`eGON.BT0 steht bei LBA 16` -> `ALL GREEN`/`Secure Storage unchanged`/`eGON.BT0 sits at LBA 16` |
| `test_mkimage_selftest.py` | red marker | `assertNotIn("FEHL")` -> `assertNotIn("FAIL")` |
| `test_mkimage_selftest.py` | `OK_LINES` | **18, unchanged** -- no check was added or dropped |
| `test_extract_manifest.py` | the three digests | **unchanged** -- only the key names moved, no value did |
| `test_extract_mips.py` | report file | `BERICHT.txt` -> `REPORT.txt` (plus an assertion that the copy is byte-identical) |
| `test_gpt.py`, `test_sparse.py` | -- | **nothing re-frozen**: their frozen values are bytes, and the `Secure Storage` marker test_sparse asserts comes out of `h713/blockdev.py`, which belongs to package D1 |

## 7. Still German on purpose

| what | where | why |
|---|---|---|
| `verzeichnis`, `groesse`, `groesse_ls`, `kurz`, `lang`, `typ`, `mode`, `uid`, `gid`, ... | h713/fs/fat16.py, h713/fs/ext4.py directory and inode entries | read by h713/dump.py and h713/facts.py, which belong to package D1 -- renaming them there is D1's call, not this package's |
| `s['extent_knoten']`, `s['verzeichnisse']`, `s['loecher']`, ... | h713/fs/ext4.py `self.stat` counters | never printed as keys; only the sentence Ext4.stats() builds from them is user-visible, and that is English now |
| `format`, `werkzeug`, `erzeugt`, `abbild`, `sektorgroesse`, `disk_sektoren`, `partitionen`, `sektoren`, `loch`, `warum`, `teile`, `datei`, `bausteine`, `platzhalter*`, `ziel`, `laenge`, `quelle`, `fuellung`, `sha256_muster` | the image table `*.tabelle.json` written by h713/mkimage.py | api-stufe3.md: table keys stay (`hy310-abbild-tabelle` included) until the release format changes in stage 4. Only the prose VALUE of `loch.warum` and the tool name in `werkzeug` became English |
| `"zeilenumbrueche"` / `"muster"` | value of `platzhalter_info.<n>.fuellung` | enum-like tokens that belong to the German key next to them; they go with the keys in stage 4 |
| `HY310-PLATZHALTER %s -- hy310-install fuellt das. ` | h713/layout.py `pattern()` | NOT a printed line but image content: it stands in every built image, its sha256 is the table's `sha256_muster`, and `h713-mkimage check` decides from it whether a placeholder is unfilled. Translating it would make every pre-stage-3 image read as 'already filled' |
| `BERICHT.txt` | h713/extract.py write_manifest() | written as a byte-identical copy of REPORT.txt for one release (api-stufe3.md) |
| `*.tabelle.json`, `*-a-bootkette.img`, `*-b-system.img`, `*-c-gptkopie.img` | release file names | renamed with the release format in v0.6, not here |

One value is still German although its key is ours: `device_description` /
`MANIFEST.json`'s board one-liner used to come from `h713/profiles` `LEGACY_DESCRIPTIONS`.
Stage 3 reads the profile's English `description` instead (`h713.extract.description_of()`),
so nothing German reaches the manifest; the German table itself lives in `h713/profiles`,
which is not this package's to delete.
