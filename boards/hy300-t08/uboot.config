# HY300 T08 board fragment. Probe builds only -- nobody has run this.
#
# DRAM: DDR3 (type 3), 640 MHz. Values are the 24 words of the vendor boot0
# header at boot0_sdcard.fex+0x38 of HY300_T08_OTA_2024-04-19-2028.img, as
# recorded in installer/h713/profiles/hy300_t08.py ("dram"). They come from an
# image, not from a device, so the two words the flashing tool patches are
# marked below; everything else is this board's own.

CONFIG_DRAM_CLK=640
CONFIG_SUNXI_DRAM_H713_DDR3_STOCK=y
# not this board's: DX_ODT, DX_DRI and CA_DRI are not part of the 24-word boot0
# block. These are the values every H713 defconfig in the fork carries.
CONFIG_DRAM_SUNXI_DX_ODT=0x03030303
CONFIG_DRAM_SUNXI_DX_DRI=0x0e0e0e0e
CONFIG_DRAM_SUNXI_CA_DRI=0x1c12
CONFIG_DRAM_SUNXI_ODT_EN=0x1
CONFIG_DRAM_SUNXI_ZQ=0x007b7bfb
CONFIG_DRAM_SUNXI_PARA1=0x000010f4
# not this board's: the image says para2 = 0x00000000, which is the unpatched
# form. The flashing tool writes the real value onto the eMMC; 0x04000000 is
# what the HY310 runs (docs/tools/h713-probe.md). Replace it with this board's
# own para2 from an h713_probe log before trusting the fragment.
CONFIG_DRAM_SUNXI_PARA2=0x04000000
CONFIG_DRAM_SUNXI_MR0=0x00001c70
CONFIG_DRAM_SUNXI_MR1=0x00000040
CONFIG_DRAM_SUNXI_MR2=0x00000018
CONFIG_DRAM_SUNXI_MR3=0x00000000
# tpr0..tpr2 are this board's own, but they do not reach the hardware on the
# DDR3 path: iw12_set_timing() computes the DDR3 timing registers from
# CONFIG_DRAM_CLK (arch/arm/mach-sunxi/dram_sun50iw12.c).
CONFIG_DRAM_SUNXI_TPR0=0x004a2195
CONFIG_DRAM_SUNXI_TPR1=0x02423190
CONFIG_DRAM_SUNXI_TPR2=0x0008b061
CONFIG_DRAM_SUNXI_TPR3=0xb4787896
CONFIG_DRAM_SUNXI_TPR4=0x00000000
CONFIG_DRAM_SUNXI_TPR5=0x48484848
CONFIG_DRAM_SUNXI_TPR6=0x00000048
CONFIG_DRAM_SUNXI_TPR7=0x1620121e
CONFIG_DRAM_SUNXI_TPR8=0x00000000
CONFIG_DRAM_SUNXI_TPR9=0x00000000
CONFIG_DRAM_SUNXI_TPR10=0x00000000
# per-board PHY tuning, this board's own (HY310: 0x44340000 / 0x00006666)
CONFIG_DRAM_SUNXI_TPR11=0x44440000
CONFIG_DRAM_SUNXI_TPR12=0x00005555
# not this board's: the image says tpr13 = 0x34010100 (unpatched, the same
# value the HY310 image carries); 0xb4016103 is the patched pair of the HY310.
CONFIG_DRAM_SUNXI_TPR13=0xb4016103

# not this board's: we have no DTS for the HY300 T08. A probe build never drives
# the panel, and no release is built from this fragment (STATUS=profile-only).
CONFIG_DEFAULT_DEVICE_TREE="allwinner/sun50i-h713-hy200-qz713df-a1"
CONFIG_IDENT_STRING=" HY300 T08 (probe, untested board)"
