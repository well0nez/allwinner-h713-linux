# zram-Modul beim Start laden. num_devices bleibt bei 1 -- wir legen genau ein
# Blockgeraet an (siehe hy310-zram-swap).
#
# ACHTUNG, Stand 10.09.2026: der Kernel kann das NOCH NICHT. Im Defconfig
# steht nur CONFIG_SWAP=y; es fehlen CONFIG_ZRAM, CONFIG_ZSMALLOC,
# CONFIG_ZRAM_BACKEND_ZSTD und CONFIG_ZRAM_DEF_COMP_ZSTD (107 §4.2, dort ist
# ein Defconfig-Fragment in patches/kernel/board/ vorgesehen). Bis das drin
# ist, laeuft die Unit ins Leere -- absichtlich ohne Fehler, siehe dort.
zram
